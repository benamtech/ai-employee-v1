# AMTECH UI Variant Context

@README.md
@AGENTS.md

When working in a specific variant, also read that folder's `instructions.md`, `TASK.md`, and `variant.json`. Treat files outside the selected variant directory as read-only during ordinary design work.
