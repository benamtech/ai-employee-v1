#!/usr/bin/env node
import { mkdtemp, mkdir, rm, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { dirname, relative, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { execFileSync } from 'node:child_process';
import { computeEffectFrontier } from './analyze/effect-frontier.mjs';
import { computeSpectralCertificate } from './analyze/spectral-hypergraph.mjs';
import { computeTaskDiffusion } from './analyze/task-diffusion.mjs';
import { extractRepositoryFacts } from './extract/repository-facts.mjs';
import { ENGINE_VERSION, artifactEnvelope, digestObject, fileExists, gitHead, gitRoot, gitStatus, parseCli, readJson, resolveWithin, runCommand, stableStringify, uniqueSorted, validateIdentifier, writeJson, writeText } from './lib/core.mjs';
import { buildAuthorityDag } from './represent/authority-dag.mjs';
import { buildDependencyGraph } from './represent/dependency-graph.mjs';
import { buildCorrespondence, buildInvariantHypergraph } from './represent/invariant-hypergraph.mjs';
import { verifyClaimCertificateDirectory } from './verify/verify-certificate.mjs';
import { verifyTransactionDirectory } from './verify/verify-transaction.mjs';

const ENGINE_DIR=dirname(fileURLToPath(import.meta.url));
const REGISTRY_PATH=resolve(ENGINE_DIR,'representation-registry.json');
const N={facts:'repository-facts.json',authority:'authority-dag.json',dependency:'dependency-graph.json',hypergraph:'invariant-hypergraph.json',correspondence:'correspondence.json',spectral:'spectral-certificate.json',claim:'claim-certificate.json',diffusion:'task-diffusion.json',effects:'effect-frontier.json',experimentInitial:'experiment.initial.json',experiment:'experiment.json',capsule:'task-capsule.json',registry:'representation-registry.snapshot.json'};
function required(o,n){const v=o[n];if(v===undefined||v===true||v==='')throw new Error(`--${n} is required`);return String(v);}
function rootOf(o){return resolve(String(o.root??process.cwd()));}
function txPath(root,o,id){return resolveWithin(root,String(o.out??o.transaction??`decision/transactions/${id}`));}
function outside(root,dir){const repo=gitRoot(root);const prefix=relative(repo,dir).split('\\').join('/').replace(/\/$/,'');return gitStatus(repo).filter((item)=>{const p=item.path.replace(/\/$/,'');return !(p===prefix||p.startsWith(`${prefix}/`)||prefix.startsWith(`${p}/`));});}
function predictions(task,effects){if(Array.isArray(task.predictions)&&task.predictions.length)return task.predictions.map((p,i)=>({id:p.id??`prediction-${i+1}`,...p}));const count=effects?.payload?.affected_files?.length??0;return[
{id:'prediction-impact-recall',horizon:1,type:'direct',claim:'Every changed source path is present in the generated impact frontier or explicitly recorded as discovered later.',falsifier:'The final diff contains an uncovered path.',metric:'uncovered_changed_paths',expected:0},
{id:'prediction-invariant-coverage',horizon:2,type:'verification',claim:'Every hard invariant hyperedge touched by the change has an accepted proof obligation or an explicit blocker.',falsifier:'A touched hard hyperedge has neither accepted evidence nor a blocker.',metric:'unresolved_hard_hyperedges',expected:0},
{id:'prediction-authority-consistency',horizon:3,type:'architecture',claim:'The change preserves one routed authority chain without a competing current-status owner.',falsifier:'Authority verification finds a cycle, duplicate status owner, or stale active router.',metric:'authority_errors',expected:0},
{id:'prediction-evidence-nonpromotion',horizon:4,type:'production-system',claim:'The final handoff does not promote model, source, CI, or fixture evidence into an unearned external class.',falsifier:'The evidence ledger claims a class stronger than its evidence.',metric:'evidence_promotions',expected:0}
].map((p)=>({...p,context_hint_count:count}));}
function triggered(task){const text=`${task.goal??''} ${(task.task_seeds??[]).join(' ')}`.toLowerCase();const rules=[[['retry','concurrency','lifecycle','state machine','transition'],'transition.system.v1'],[['constraint','boolean','satisfiability','resource allocation'],'constraint.smt.v1'],[['rewrite','equivalent transformation','phase order'],'rewrite.egraph.v1'],[['queue','capacity','service rate','fairness'],'queueing.state.v1'],[['trajectory','episode','koopman','dmd'],'koopman.episode.v1'],[['simplicial','hodge','homology','oriented face'],'topology.complex.v1'],[['embedding','semantic retrieval','duplicate detection'],'embedding.code-change.v1']];return rules.filter(([terms])=>terms.some((term)=>text.includes(term))).map(([,id])=>id);}
function experiment(sourceSha,task,preds,status='draft',lineage=null,outcomes=[]){return artifactEnvelope('experiment.design.v1',sourceSha,{task_id:task.task_id,status,baseline:task.baseline??{id:'evidence-and-invariants',same_prerequisite_domain:true},metrics:task.metrics??['changed_file_recall','required_test_recall','missed_invariant_edges','unnecessary_scope','time_to_valid_plan','time_to_exact_head_green','review_defects','context_volume'],predictions:preds,outcomes,evidence_classes:['P0','P1','P2','P3','P4'],stop_conditions:task.stop_conditions??['hard invariant violation','red exact-head gate','evidence-class promotion','unbounded patch scope'],lineage},{generator:'decision/engine/repoctl.mjs',chronology:'predictions are recorded before plan admission and outcomes'});}
function capsuleText(c,e,fx){const p=c.payload;return `# Task capsule — ${p.task_id}\n\nBase SHA: \`${p.base_sha}\`  \nGoal: ${p.goal}\n\n## Machine authority\n\n- facts: \`${p.repository_fact_digest}\`\n- authority DAG: \`${p.authority_digest}\`\n- invariant hypergraph: \`${p.invariant_hypergraph_digest}\`\n- task diffusion: \`${p.diffusion_digest}\`\n\n## Mandatory context\n\n${p.mandatory_context.slice(0,30).map((i)=>`- \`${i.path}\` — ${i.score.toFixed(6)}`).join('\n')}\n\n## Hard invariant edges\n\n${p.hard_invariants.map((id)=>`- \`${id}\``).join('\n')}\n\n## Effect frontier\n\n${[1,2,3,4].map((d)=>`- order ${d}: ${(fx.payload.by_horizon[d]??[]).length} typed relations`).join('\n')}\n\nNatural language is a review view. The JSON artifacts and their verifiers own semantics.\n`;}
async function load(dir){const r=(name)=>readJson(resolve(dir,name));return{capsule:await r(N.capsule),facts:await r(N.facts),authority:await r(N.authority),dependency:await r(N.dependency),hypergraph:await r(N.hypergraph),correspondence:await r(N.correspondence),spectral:await r(N.spectral),claim:await r(N.claim),diffusion:await r(N.diffusion),effects:await r(N.effects),experimentInitial:await r(N.experimentInitial),experiment:await r(N.experiment),registry:await r(N.registry)};}
export async function startTransaction({root,task,out,allowDirty=false}){const repo=gitRoot(root);const sha=gitHead(repo);const id=validateIdentifier(task.task_id??`TASK-${Date.now()}`,'task_id');const t={...task,task_id:id,goal:String(task.goal??'').trim()};if(!t.goal)throw new Error('task.goal is required');const dir=resolve(out);if(await fileExists(dir))throw new Error(`transaction directory already exists: ${dir}`);const initial=outside(root,dir);if(initial.length&&!allowDirty)throw new Error(`working tree is not clean; use --allow-dirty only when intentional: ${initial.map((i)=>i.path).join(', ')}`);await mkdir(dir,{recursive:true});const registry=await readJson(REGISTRY_PATH);const facts=await extractRepositoryFacts({root});const authority=buildAuthorityDag(facts);const dependency=buildDependencyGraph(facts);const hypergraph=buildInvariantHypergraph(facts,dependency,authority);const correspondence=buildCorrespondence(facts,dependency,authority,hypergraph);const{spectral,claim}=computeSpectralCertificate(hypergraph,correspondence,t.spectral??{});const diffusion=computeTaskDiffusion(t,dependency,hypergraph,t.diffusion??{});const effects=computeEffectFrontier(t,diffusion,dependency,hypergraph,{depth:4,...(t.effect_frontier??{})});const preds=predictions(t,effects);const initialExperiment=experiment(sha,t,preds);const implemented=registry.dialects.filter((d)=>d.status==='implemented').map((d)=>d.id);const selected=uniqueSorted([...registry.selector.always_on,...registry.selector.specialized_default,'task.diffusion.v1','claim.certificate.v1']).filter((x)=>implemented.includes(x));const hard=hypergraph.payload.hyperedges.filter((edge)=>edge.hard);const context=diffusion.payload.mandatory_context.map((i)=>({path:i.path,score:i.score,reasons:i.reasons}));const capsule=artifactEnvelope('task.capsule.v1',sha,{task_id:id,base_sha:sha,goal:t.goal,non_goals:t.non_goals??[],authority_digest:authority.content_digest,repository_fact_digest:facts.content_digest,invariant_hypergraph_digest:hypergraph.content_digest,diffusion_digest:diffusion.content_digest,effect_frontier_digest:effects.content_digest,task_seeds:t.task_seeds??[],selected_representations:selected,recommended_future_representations:triggered(t),mandatory_context:context,hard_invariants:hard.map((edge)=>edge.id),unknowns:t.unknowns??['external P4 prerequisites remain unknown until observed on the exact candidate'],candidate_generation_contract:t.candidate_generation_contract??{independent_batches_before_recombination:true,invariant_and_prerequisite_filter:true,simple_baseline_required:true,mathematical_layers_must_change_a_decision_or_proof_obligation:true},experiment_contract:initialExperiment.content_digest,predictions:preds.map((p)=>p.id),proof_obligations:hard.map((edge)=>({hyperedge_id:edge.id,status:'planned',candidate_evidence:edge.members.filter((m)=>m.startsWith('file:')&&/test|spec|workflow/.test(m))})),stop_conditions:initialExperiment.payload.stop_conditions,maximum_patch:t.maximum_patch??{maximum_files:30,allowed_paths:context.map((i)=>i.path).slice(0,100),expansion_rule:'additional paths require discovered_later evidence before evaluation'},allowed_evidence_classes:t.allowed_evidence_classes??['P0','P1','P2','P3'],external_blockers:t.external_blockers??['managed database','live provider','target host','human accessibility','pilot','deployment','production'],initial_dirty_paths:initial.map((i)=>i.path),transaction_directory:relative(repo,dir).split('\\').join('/')},{generator:'decision/engine/repoctl.mjs start',input_task_digest:digestObject(t),capsule_predates_source_edits:true});const artifacts={[N.facts]:facts,[N.authority]:authority,[N.dependency]:dependency,[N.hypergraph]:hypergraph,[N.correspondence]:correspondence,[N.spectral]:spectral,[N.claim]:claim,[N.diffusion]:diffusion,[N.effects]:effects,[N.experimentInitial]:initialExperiment,[N.experiment]:initialExperiment,[N.capsule]:capsule,[N.registry]:registry};for(const[name,value]of Object.entries(artifacts))await writeJson(resolve(dir,name),value);await writeJson(resolve(dir,'task.request.json'),t);await writeText(resolve(dir,'TASK-CAPSULE.md'),capsuleText(capsule,diffusion,effects));const verification=await verifyTransactionDirectory(dir,{root,phase:'start'});if(!verification.ok)throw new Error(`generated transaction failed verification: ${verification.errors.join('; ')}`);return{ok:true,task_id:id,transaction_dir:dir,source_sha:sha,capsule_digest:capsule.content_digest,selected_representations:selected,recommended_future_representations:capsule.payload.recommended_future_representations};}
function nextExperiment(previous,status,{predictions:preds=previous.payload.predictions,outcomes=previous.payload.outcomes}={}){return artifactEnvelope('experiment.design.v1',previous.source_sha,{...previous.payload,status,predictions:preds,outcomes,lineage:{root_digest:previous.payload.lineage?.root_digest??previous.content_digest,previous_digest:previous.content_digest}},{generator:'decision/engine/repoctl.mjs',chronology:'immutable root experiment retained in experiment.initial.json'});}
export async function admitPlan({root,transactionDir,plan}){const tx=await load(transactionDir);const preds=Array.isArray(plan.predictions)&&plan.predictions.length?plan.predictions:tx.experiment.payload.predictions;const current=outside(root,transactionDir).map((i)=>i.path);const initial=new Set(tx.capsule.payload.initial_dirty_paths??[]);const planPath=plan.__path?relative(gitRoot(root),plan.__path).split('\\').join('/'):null;const newPaths=current.filter((p)=>!initial.has(p)&&p!==planPath);if(newPaths.length)throw new Error(`source changed before plan admission: ${newPaths.join(', ')}`);const exp=nextExperiment(tx.experiment,'admitted',{predictions:preds,outcomes:[]});await writeJson(resolve(transactionDir,N.experiment),exp);const snapshot={...plan};delete snapshot.__path;await writeJson(resolve(transactionDir,'plan.snapshot.json'),snapshot);const admission=artifactEnvelope('plan.admission.v1',tx.capsule.source_sha,{task_id:tx.capsule.payload.task_id,capsule_digest:tx.capsule.content_digest,plan_digest:digestObject(snapshot),experiment_root_digest:tx.experimentInitial.content_digest,experiment_digest:exp.content_digest,current_head_at_admission:gitHead(root),new_paths_before_admission:newPaths,selected_candidates:plan.selected_candidates??[],proof_obligations:plan.proof_obligations??tx.capsule.payload.proof_obligations,verification_commands:plan.verification?.commands??[],discovered_later_policy:plan.discovered_later_policy??'must be recorded with evidence before evaluation'},{generator:'decision/engine/repoctl.mjs admit-plan'});await writeJson(resolve(transactionDir,'plan-admission.json'),admission);const verification=await verifyTransactionDirectory(transactionDir,{root,phase:'admitted'});if(!verification.ok)throw new Error(`plan admission failed verification: ${verification.errors.join('; ')}`);return{ok:true,task_id:tx.capsule.payload.task_id,admission_digest:admission.content_digest,experiment_digest:exp.content_digest};}
function evalPrediction(p,c){if(p.metric==='uncovered_changed_paths')return{value:c.uncovered.length,passed:c.uncovered.length===Number(p.expected??0)};if(p.metric==='unresolved_hard_hyperedges')return{value:c.unresolvedHard.length,passed:c.unresolvedHard.length===Number(p.expected??0)};if(p.metric==='authority_errors')return{value:c.authorityErrors.length,passed:c.authorityErrors.length===Number(p.expected??0)};if(p.metric==='evidence_promotions')return{value:c.promotions.length,passed:c.promotions.length===Number(p.expected??0)};return{value:null,passed:null};}
function metricEvidence(m,c){if(m==='uncovered_changed_paths')return c.uncovered;if(m==='unresolved_hard_hyperedges')return c.unresolvedHard;if(m==='authority_errors')return c.authorityErrors;if(m==='evidence_promotions')return c.promotions;return[];}
export async function evaluateTransaction({root,transactionDir,outcomes=[],skipCommands=false}){const tx=await load(transactionDir);const admission=await readJson(resolve(transactionDir,'plan-admission.json'));const plan=await readJson(resolve(transactionDir,'plan.snapshot.json'));const repo=gitRoot(root);const output=execFileSync('git',['-C',repo,'diff','--name-only',tx.capsule.payload.base_sha],{encoding:'utf8'});const prefix=relative(repo,transactionDir).split('\\').join('/').replace(/\/$/,'');const changed=uniqueSorted(output.split(/\r?\n/).filter(Boolean).filter((p)=>!(p===prefix||p.startsWith(`${prefix}/`))));const allowed=new Set([...(tx.capsule.payload.maximum_patch?.allowed_paths??[]),...(tx.effects.payload.affected_files??[]),...(plan.maximum_patch?.allowed_paths??[]),...(plan.discovered_later??[]).map((i)=>typeof i==='string'?i:i.path)]);const uncovered=changed.filter((p)=>!allowed.has(p));const max=Number(plan.maximum_patch?.maximum_files??tx.capsule.payload.maximum_patch?.maximum_files??Infinity);if(changed.length>max)uncovered.push(`__maximum_files_exceeded__:${changed.length}>${max}`);const touchedVertices=new Set(changed.map((p)=>`file:${p}`));const touchedHard=tx.hypergraph.payload.hyperedges.filter((edge)=>edge.hard&&edge.members.some((m)=>touchedVertices.has(m)));const proofByEdge=new Map((plan.proof_obligations??admission.payload.proof_obligations??[]).map((i)=>[i.hyperedge_id,i]));const unresolvedHard=touchedHard.filter((edge)=>!['accepted','blocked','planned'].includes(proofByEdge.get(edge.id)?.status)).map((edge)=>edge.id);const commands=plan.verification?.commands??admission.payload.verification_commands??[];const verificationResults=skipCommands?[]:commands.map((entry)=>runCommand(root,entry.argv,{timeoutMs:entry.timeout_ms??15*60_000}));const authorityErrors=tx.authority.payload.cycles??[];const promotions=[];const maximumEvidenceClass=verificationResults.length&&verificationResults.every((r)=>r.ok)?'P3':'P2';if(!tx.capsule.payload.allowed_evidence_classes.includes(maximumEvidenceClass))promotions.push(`not_allowed:${maximumEvidenceClass}`);const context={uncovered,unresolvedHard,authorityErrors,promotions};const supplied=new Map(outcomes.map((o)=>[o.prediction_id,o]));const predictionOutcomes=tx.experiment.payload.predictions.map((p)=>supplied.get(p.id)??{prediction_id:p.id,observed:evalPrediction(p,context),evidence:metricEvidence(p.metric,context),source:'repoctl-auto-evaluation'});const claimResult=await verifyClaimCertificateDirectory(transactionDir);const ledger=artifactEnvelope('evidence.ledger.v1',gitHead(repo),{task_id:tx.capsule.payload.task_id,base_sha:tx.capsule.payload.base_sha,evaluated_sha:gitHead(repo),changed_paths:changed,impact_coverage:{allowed_paths:uniqueSorted([...allowed]),uncovered_paths:uniqueSorted(uncovered),discovered_later:plan.discovered_later??[]},touched_hard_hyperedges:touchedHard.map((edge)=>edge.id),unresolved_hard_hyperedges:unresolvedHard,verification_results:verificationResults,prediction_outcomes:predictionOutcomes,claims:[{claim_id:tx.claim.payload.claim_id,valid:claimResult.ok,evidence_class:tx.claim.payload.claim.evidence_class,errors:claimResult.errors}],external_blockers:tx.capsule.payload.external_blockers,maximum_evidence_class:maximumEvidenceClass,evidence_promotions:promotions},{generator:'decision/engine/repoctl.mjs evaluate',exact_candidate:gitHead(repo)});await writeJson(resolve(transactionDir,'evidence-ledger.json'),ledger);const exp=nextExperiment(tx.experiment,'evaluated',{predictions:tx.experiment.payload.predictions,outcomes:predictionOutcomes});await writeJson(resolve(transactionDir,N.experiment),exp);const verification=await verifyTransactionDirectory(transactionDir,{root,phase:'evaluated'});if(!verification.ok)throw new Error(`evaluation failed verification: ${verification.errors.join('; ')}`);return{ok:true,task_id:tx.capsule.payload.task_id,ledger_digest:ledger.content_digest,maximum_evidence_class:maximumEvidenceClass,changed_paths:changed.length,uncovered_paths:uncovered,verification_results:verificationResults.map(({argv,ok,status,duration_ms})=>({argv,ok,status,duration_ms}))};}
export async function finishTransaction({root,transactionDir}){const tx=await load(transactionDir);const ledger=await readJson(resolve(transactionDir,'evidence-ledger.json'));const exp=nextExperiment(tx.experiment,'finished',{predictions:tx.experiment.payload.predictions,outcomes:tx.experiment.payload.outcomes});await writeJson(resolve(transactionDir,N.experiment),exp);const final=artifactEnvelope('experiment.transaction.v1',gitHead(root),{task_id:tx.capsule.payload.task_id,status:'finished',base_sha:tx.capsule.payload.base_sha,final_sha:gitHead(root),capsule_digest:tx.capsule.content_digest,plan_admission_digest:(await readJson(resolve(transactionDir,'plan-admission.json'))).content_digest,evidence_ledger_digest:ledger.content_digest,experiment_digest:exp.content_digest,certificate_digest:tx.claim.content_digest,correspondence_digest:tx.correspondence.content_digest,maximum_evidence_class:ledger.payload.maximum_evidence_class,external_blockers:ledger.payload.external_blockers,outcome_calibration:exp.payload.outcomes},{generator:'decision/engine/repoctl.mjs finish'});await writeJson(resolve(transactionDir,'transaction.json'),final);const verification=await verifyTransactionDirectory(transactionDir,{root,phase:'finished'});if(!verification.ok)throw new Error(`final transaction failed verification: ${verification.errors.join('; ')}`);await writeText(resolve(transactionDir,'HANDOFF.md'),`# Experiment handoff — ${tx.capsule.payload.task_id}\n\n- base: \`${tx.capsule.payload.base_sha}\`\n- final: \`${gitHead(root)}\`\n- maximum evidence class: \`${ledger.payload.maximum_evidence_class}\`\n- P1 certificate: ${tx.claim.payload.claim.statement}\n- P2 correspondence: ${tx.correspondence.payload.status}\n- external blockers: ${ledger.payload.external_blockers.join(', ')}\n\nThe machine artifacts and small verifiers are canonical for this transaction.\n`);return{ok:true,task_id:tx.capsule.payload.task_id,transaction_digest:final.content_digest,maximum_evidence_class:ledger.payload.maximum_evidence_class};}
async function query(dir,q,o){const tx=await load(dir);const value=o.entity??o.path??o.hyperedge??o.claim??o.candidate??'';if(q==='authority'){const t=String(value).toLowerCase();return{nodes:tx.authority.payload.nodes.filter((n)=>JSON.stringify(n).toLowerCase().includes(t)),edges:tx.authority.payload.edges.filter((e)=>JSON.stringify(e).toLowerCase().includes(t))};}if(q==='impact'){const id=String(value).startsWith('file:')?String(value):`file:${value}`;return{id,edges:tx.dependency.payload.edges.filter((e)=>e.from===id||e.to===id),hyperedges:tx.hypergraph.payload.hyperedges.filter((e)=>e.members.includes(id)),effects:tx.effects.payload.effects.filter((e)=>e.from===id||e.to===id||e.origin===id)};}if(q==='invariants'){const t=String(value);return tx.hypergraph.payload.hyperedges.filter((e)=>e.id.includes(t)||e.members.some((m)=>m.includes(t)));}if(q==='proofs'){const id=String(value);return{hyperedge:tx.hypergraph.payload.hyperedges.find((e)=>e.id===id)??null,correspondence:tx.correspondence.payload.mappings.find((m)=>m.representation_id===id)??null,obligation:tx.capsule.payload.proof_obligations.find((i)=>i.hyperedge_id===id)??null};}if(q==='effects'){const depth=Math.min(4,Number(o.depth??4));const t=String(value);return tx.effects.payload.effects.filter((e)=>e.horizon<=depth&&(!t||JSON.stringify(e).includes(t)));}if(q==='evidence'){const ledger=resolve(dir,'evidence-ledger.json');return{claim:JSON.stringify(tx.claim).includes(String(value))?tx.claim:null,ledger:await fileExists(ledger)?await readJson(ledger):null};}throw new Error(`unknown query: ${q}`);}
async function doctor(){const registry=await readJson(REGISTRY_PATH);const errors=[];const ids=new Set();for(const d of registry.dialects??[]){if(ids.has(d.id))errors.push(`duplicate_dialect:${d.id}`);ids.add(d.id);if(d.status==='implemented'){if(!d.generator)errors.push(`implemented_generator_missing:${d.id}`);else if(!(await fileExists(resolve(ENGINE_DIR,d.generator))))errors.push(`generator_not_found:${d.id}:${d.generator}`);if(!d.verifier)errors.push(`implemented_verifier_missing:${d.id}`);else if(!(await fileExists(resolve(ENGINE_DIR,d.verifier))))errors.push(`verifier_not_found:${d.id}:${d.verifier}`);}}for(const id of [...registry.selector.always_on,...registry.selector.specialized_default])if(!ids.has(id))errors.push(`selector_unknown_dialect:${id}`);for(const schema of ['repository-facts','task-capsule','claim-certificate','correspondence','experiment','evidence-ledger'])if(!(await fileExists(resolve(ENGINE_DIR,'schemas',`${schema}.schema.json`))))errors.push(`schema_missing:${schema}`);return{ok:errors.length===0,errors,engine_version:ENGINE_VERSION,implemented_dialects:registry.dialects.filter((i)=>i.status==='implemented').map((i)=>i.id)};}
async function selfTest(){const root=await mkdtemp(resolve(tmpdir(),'amtech-experiment-engine-'));try{await mkdir(resolve(root,'src'),{recursive:true});await mkdir(resolve(root,'tests'),{recursive:true});await mkdir(resolve(root,'decision'),{recursive:true});await writeFile(resolve(root,'src/b.mjs'),'export const value = 1;\n');await writeFile(resolve(root,'src/a.mjs'),"import { value } from './b.mjs';\nexport function readValue(){ return value; }\n");await writeFile(resolve(root,'tests/a.test.mjs'),"import { readValue } from '../src/a.mjs';\nif (readValue() !== 1) process.exit(1);\n");await writeFile(resolve(root,'AGENTS.md'),'# AGENTS\nRead `CODEGRAPH.md` and `decision/active.json`.\n');await writeFile(resolve(root,'CODEGRAPH.md'),'# CODEGRAPH\nCurrent authority.\n');await writeFile(resolve(root,'decision/active.json'),'{"status":"active"}\n');await writeFile(resolve(root,'authority-map.json'),JSON.stringify({exact_status:{owner:'CODEGRAPH.md'},agent_entry:['AGENTS.md'],decision:{router:'decision/active.json'},historical:[]},null,2));await writeFile(resolve(root,'package.json'),JSON.stringify({type:'module',scripts:{test:'node tests/a.test.mjs'}},null,2));execFileSync('git',['-C',root,'init','-q']);execFileSync('git',['-C',root,'config','user.email','selftest@example.com']);execFileSync('git',['-C',root,'config','user.name','Self Test']);execFileSync('git',['-C',root,'add','.']);execFileSync('git',['-C',root,'commit','-qm','fixture']);const task={task_id:'SELFTEST-ENGINE',goal:'Change src/a.mjs while preserving imports, tests, and authority routing',paths:['src/a.mjs']};const dir=resolve(root,'decision/transactions/SELFTEST-ENGINE');const started=await startTransaction({root,task,out:dir});const planPath=resolve(root,'plan.json');const plan={selected_candidates:['SELFTEST-C1'],maximum_patch:{maximum_files:2,allowed_paths:['src/a.mjs']},verification:{commands:[{argv:['node','tests/a.test.mjs']}]},__path:planPath};await writeJson(planPath,{...plan,__path:undefined});const admitted=await admitPlan({root,transactionDir:dir,plan});await writeFile(resolve(root,'src/a.mjs'),"import { value } from './b.mjs';\nexport function readValue(){ return Number(value); }\n");const evaluated=await evaluateTransaction({root,transactionDir:dir});const finished=await finishTransaction({root,transactionDir:dir});const verified=await verifyTransactionDirectory(dir,{root,phase:'finished'});return{ok:started.ok&&admitted.ok&&evaluated.ok&&finished.ok&&verified.ok,started,admitted,evaluated,finished,verified};}finally{await rm(root,{recursive:true,force:true});}}
async function main(){const{positionals,options}=parseCli(process.argv.slice(2));const command=positionals[0];if(!command||['help','--help','-h'].includes(command)){console.log(`repoctl ${ENGINE_VERSION}\n\nCommands:\n  start --task task.json [--out path] [--allow-dirty]\n  admit-plan --transaction path --plan plan.json\n  evaluate --transaction path [--outcomes outcomes.json] [--skip-commands]\n  finish --transaction path\n  verify --transaction path [--phase start|admitted|evaluated|finished]\n  query <authority|impact|invariants|proofs|effects|evidence> --transaction path [selector]\n  doctor\n  self-test`);return;}const root=rootOf(options);let result;if(command==='start'){const task=await readJson(resolve(root,required(options,'task')));result=await startTransaction({root,task,out:txPath(root,options,task.task_id??`TASK-${Date.now()}`),allowDirty:Boolean(options['allow-dirty'])});}else if(command==='admit-plan'){const path=resolve(root,required(options,'plan'));result=await admitPlan({root,transactionDir:txPath(root,options,'unused'),plan:{...(await readJson(path)),__path:path}});}else if(command==='evaluate'){result=await evaluateTransaction({root,transactionDir:txPath(root,options,'unused'),outcomes:options.outcomes?await readJson(resolve(root,String(options.outcomes))):[],skipCommands:Boolean(options['skip-commands'])});}else if(command==='finish')result=await finishTransaction({root,transactionDir:txPath(root,options,'unused')});else if(command==='verify')result=await verifyTransactionDirectory(txPath(root,options,'unused'),{root,phase:String(options.phase??'auto')});else if(command==='query')result=await query(txPath(root,options,'unused'),positionals[1],options);else if(command==='doctor')result=await doctor();else if(command==='self-test')result=await selfTest();else throw new Error(`unknown command: ${command}`);console.log(stableStringify(result));if(result?.ok===false)process.exitCode=1;}
if(process.argv[1]&&resolve(process.argv[1])===fileURLToPath(import.meta.url))main().catch((error)=>{console.error(JSON.stringify({ok:false,error:error.message,stack:error.stack},null,2));process.exitCode=1;});
