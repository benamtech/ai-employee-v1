#!/usr/bin/env node

/**
 * Compatibility entrypoint retained for workflows and documentation.
 * The workbench-aware harness owns the current production owner + UI Lab matrix.
 */
await import("./fixture-browser-v2.mjs");
