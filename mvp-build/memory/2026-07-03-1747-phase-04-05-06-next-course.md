# Handoff — Phase 4/5/6 Next Course After Live Employee Core

Date: 2026-07-03 17:47
Status: planning handoff; recent Phase 3/3A/4-core changes are source-wired, live proof pending
Scope: review recent uncommitted changes, map them against `wiki/MVP/build-plan-current/phases/`, and choose the next implementation sequence for Phase 4 completion, Phase 5, and possible Phase 6.

## What changed

Recent local changes in `mvp-build/` materially advance Phase 3, Phase 3A, and Phase 4-core:

- Phase 3: generic ingress is now the hot path via `apps/manager/src/events/ingress.ts` and adapters in `apps/manager/src/events/adapters/` for Gmail, Stripe, and Manager sources.
- Phase 3A: `apps/manager/src/lib/channel-router.ts`, `apps/web/app/api/employee/[employeeId]/heartbeat/route.ts`, and migration `packages/db/migrations/0012_phase3a_channel_router.sql` add minimal web/SMS presence, `delivery_decisions`, active-web-wins routing, silent intents, and cross-channel dedupe.
- Phase 4-core: `apps/manager/src/lib/hermes-client.ts`, `turn-queue.ts`, `wake.ts`, and migration `0011_phase4_hermes_runtime.sql` replace fictional Hermes runtime calls with real API Server health/capabilities/session/chat, sealed runtime API keys, canonical sessions, and DB-backed serialized turn jobs.
- Gmail reply flow now claims an event, wakes Hermes, parses/validates a JSON `WorkEventDescriptor`, stamps identity/proof, binds approvals, and routes the employee-authored intent through the channel router.
- Acceptance docs/hooks were added or updated: `tests/golden-path/live-employee-wake.md`, `infra/scripts/acceptance/run9-live-employee.mjs`, `wiki/MVP/implementation-records/2026-07-03-phase-03-03a-04-core-record.md`, and `memory/2026-07-03-1332-phase-03-03a-04-core-source-wired.md`.

## Why

The architecture audit found three blockers: invented Hermes `/messages`/`/events/work` assumptions, a partially dead/off-rail ingress registry, and presence-blind delivery. The current changes make the live employee path testable against the real Hermes Sessions API while preserving the Manager as the invisible control plane and keeping source/channel/runtime concerns separate.

## Current status

- Phase 3: `source-wired`; provider proof pending.
- Phase 3A: `source-wired`; provider proof pending.
- Phase 4: core `source-wired`; `runtime-accepted` pending real Hermes proof ids.
- Phase 5: still `planned`.
- Phase 6: still `planned`, but now more valuable because Phase 4 introduced turn jobs and runtime calls where `run_id` should be threaded.

Do not upgrade statuses without real proof ids. Note that some older status tables may still say 25/124 tests or planned/pending generically; the newest implementation record reports 26 unit files / 128 tests and Phase 3/3A/4-core source-wired.

## Files / seams touched

Representative review targets for the next agent:

- Runtime/session: `apps/manager/src/lib/hermes-client.ts`, `apps/manager/src/lib/runtime.ts`, `apps/manager/src/lib/runtime-health.ts`, `apps/manager/src/lib/turn-queue.ts`, `apps/manager/src/lib/wake.ts`.
- Event ingress/delivery: `apps/manager/src/events/ingress.ts`, `apps/manager/src/events/adapters/*`, `apps/manager/src/lib/employee-events.ts`.
- Channel/presence: `apps/manager/src/lib/channel-router.ts`, `apps/manager/src/lib/sms-sender.ts`, `apps/web/app/agent/[employeeId]/AgentClient.tsx`, heartbeat route.
- Schema: migrations `0011_phase4_hermes_runtime.sql` and `0012_phase3a_channel_router.sql`.
- Planning docs: `wiki/MVP/build-plan-current/phases/phase-04-live-wake-path-descriptors.md`, `phase-05-triage-batching-work-surface-stream.md`, `phase-06-metering-foundation.md`, and `wiki/MVP/build-plan-current/04-admin-and-metering-plan.md`.

## Carry-forward / next

Recommended sequence:

1. First, audit the uncommitted diff and rerun local verification. Confirm the recent source state really matches the phase docs before building more on top of it.
2. Finish Phase 4 before declaring Phase 5 done. Required work is live Hermes proof (`/health`, `/v1/capabilities`, session/chat), live Gmail/Twilio/Stripe proof through generic ingress/router, malformed descriptor rejection/audit proof, RLS/grant review for new tables, and proof ids in the implementation record.
3. Harden Phase 4 where source review shows thin spots: add direct channel-router tests for active web vs SMS fallback and dedupe, decide whether queued employee turns need an always-on worker drain beyond opportunistic request-path claims, and make failed wake/descriptor repair behavior operator-visible enough for Phase 5.
4. Then implement Phase 5 on the stable descriptor/router seam: real triage priority/grouping, batch/digest flush through `delivery_decisions`, a live Work Surface stream with catch-up from persisted descriptors/messages, and bound approval cards sourced from the same descriptor record used for SMS.
5. Start Phase 6 if Phase 4 proof is blocked by credentials or while Phase 5 UI work proceeds. Phase 6 should be additive: create metering ledgers and `apps/manager/src/lib/metering.ts`, then thread a `run_id` from ingress/wake/turn jobs into descriptors, delivery decisions, audit, and future stream events. Defer broad Phase 7 instrumentation until the `run_id` contract is stable.
6. Do not jump to Phase 8 yet. Rollups/budgets need populated Phase 7 ledgers; building them before `run_id` and chokepoint instrumentation would create fake summaries.

Key reasoning:

- Recent changes reduce Phase 5 risk by giving it validated descriptors and delivery decisions, but Phase 5 still depends on live runtime acceptance for trustworthy streaming semantics.
- Phase 6 is the best "maybe next" parallel phase because `run_id` should be designed before Phase 5 stream/event shapes fossilize.
- Phase 7/8 are premature until Phase 6 exists and the live wake path proves where real work chains start/end.

## Verification

Not rerun during this planning handoff. The previous implementation record reports:

- `npm run typecheck` — pass.
- `npm run test:unit` — 26 files / 128 tests pass.
- `npm run build` — pass.
- `npm run lint` — pass.
- `npm run test:integration` — pass with 4 env-gated tests skipped.

The next agent should rerun at least `npm run typecheck`, `npm run test:unit`, `npm run build`, `npm run lint`, and targeted integration/smoke hooks before and after changes.

## Token-efficient Opus handoff prompt

Use this in a fresh session:

```text
You are in /home/georgej/AMTECH/GTM-RESEARCH. The active repo is mvp-build/; build plan docs are in ../wiki/MVP/build-plan-current/ and phase docs in ../wiki/MVP/build-plan-current/phases/.

Task: audit recent uncommitted Phase 3/3A/4-core changes, then decide and implement the best next steps to finish Phase 4, Phase 5, and maybe Phase 6.

Read first, in this order:
1. mvp-build/memory/2026-07-03-1747-phase-04-05-06-next-course.md
2. mvp-build/memory/2026-07-03-1332-phase-03-03a-04-core-source-wired.md
3. wiki/MVP/implementation-records/2026-07-03-phase-03-03a-04-core-record.md
4. wiki/MVP/build-plan-current/phases/phase-04-live-wake-path-descriptors.md
5. wiki/MVP/build-plan-current/phases/phase-05-triage-batching-work-surface-stream.md
6. wiki/MVP/build-plan-current/phases/phase-06-metering-foundation.md
7. wiki/MVP/build-plan-current/04-admin-and-metering-plan.md only for Phase 6 details.

Then inspect git status/diff in mvp-build. Key files to audit: apps/manager/src/lib/hermes-client.ts, turn-queue.ts, wake.ts, employee-events.ts, channel-router.ts, events/ingress.ts, events/adapters/*, migrations 0011/0012, Work Surface heartbeat/client, tests/golden-path/live-employee-wake.md, and package scripts.

Current understanding: Phase 3 and 3A are source-wired. Phase 4 core is source-wired but runtime/provider proof is pending. Phase 5 is planned. Phase 6 is planned and may be useful soon because run_id should thread through the new ingress/wake/turn queue seams before Phase 5 stream shapes harden.

Best likely course:
- First verify/audit recent changes and rerun local checks.
- Finish Phase 4 acceptance/hardening: live proof hooks/docs, descriptor rejection/repair audit, channel-router tests, new-table RLS/grants, decide if an always-on turn worker is needed.
- Then build Phase 5: real triage/batching, live Work Surface stream with catch-up, bound approval cards from the same descriptor/delivery records, SMS/web rendering from one descriptor.
- If blocked on live credentials or before stream contracts fossilize, start Phase 6 foundation only: additive ledgers, metering.ts, run_id propagation through ingress/wake/turn/delivery/audit. Do not start Phase 8 before Phase 7 ledgers exist.

Use the repo’s existing patterns. Do not claim provider-accepted/runtime-accepted without proof ids. Preserve user changes. Update memory/MEMORY.md and implementation records after substantial work. Run and report verification.
```
