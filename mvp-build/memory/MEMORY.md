# mvp-build durable memory — index + writing protocol

Status: active

This folder is the **in-repo durable memory for agentic development of the AI Employee MVP**. It is
the narrative handoff layer between sessions/agents: what changed, why, current status, and what the
next agent inherits. It is **versioned with the code** (committed), so a fresh agent can orient from
the repo alone.

It is distinct from, and points to, the two other memory layers:
- **`../wiki/MVP/implementation-records/`** — the factual code-state ledger (what is wired, proof ids).
- **`../wiki/MVP/build-plan-current/`** — the forward plan (phases) and architecture.
Memory handoffs here are the **agentic-dev narrative + decisions + pointers**; they do not duplicate
the records — they link to them.

## Index (newest first)

- [2026-07-12 11:09 — Live Anthropic normal onboarding handoff](2026-07-12-1109-live-anthropic-normal-onboarding-handoff.md) — PR #7 merged to `main`: Anthropic provider support for front-door onboarding and rendered Hermes employees, live stack bridge opt-in only, deterministic Binghamton painting-contractor fixture, and a headed-client handoff prompt for normal owner-facing employee proof. Live Anthropic key remains only in gitignored `.env`; actual end-to-end onboarding/runtime/webchat proof is next.
- [2026-07-12 23:59 — Profile-generator-first estimator probe source-wired](2026-07-12-2359-profile-generator-first-estimator-probe.md) — Replaced the custom purpose-package generator with `infra/scripts/local/profile-generator-harness.mjs`: normal B2B onboarding brief -> `profile-architect` prompt/params -> generated Hermes profile repo -> AMTECH package adapter/provisioner inputs. CLI/live use now fails closed unless production-shaped local infra is up (Docker backend, Hermes image/CLI, Caddy, env, Manager/Web). Added reusable adapter support for `.env.tpl`, hooks, memories, and plugins; generated package `website_estimator_conversation` registered and provisioned as live Docker employee `emp_5omv4ihbvggc8ibe31nj43` with Manager MCP wired. Do not count bridge-backed chat as proof; real LLM/tool-loop, artifact creation, multimodal, and PDF proof remain pending.
- [2026-07-12 23:59 — Purpose employee factory probe source-wired](2026-07-12-2359-purpose-employee-factory-probe-source-wired.md) — Superseded intermediate path. It generated a purpose package directly from config; founder clarified the research should stay closer to normal B2B onboarding and the Hermes profile generator.
- [2026-07-12 23:55 — CE-4 connector/operator policy + estimator materialization research](2026-07-12-2355-ce4-and-estimator-materialization-research.md) — Finished CE-4 as `source-wired`: shared connector registry/custody policy, read-only direct-MCP render filtering, write/money/customer-facing Manager mediation, generic direct read-only capability projection, Connected-surface custody copy, and business/operator policy seam for primer + rotation. Wrote `second-half-plan/estimator-product-limits-research.md`: the form is the conversation; one generated employee can materialize as a local website estimator chat with a small visitor-session adapter, while visitor-session isolation and deterministic HTML-to-PDF are the main missing primitives. Full baseline green: typecheck, 89 unit files / 558 tests, build, lint, plugins 10/10. Live Hermes/direct-MCP/provisioned estimator proof remains pending.
- [2026-07-12 23:30 — CE-2 + CE-3 production implementation + CE-1 loose ends](2026-07-12-2330-ce2-ce3-production-implemented.md) — `source-wired`; migrations `0029`(renamed `session_key`→`transcript_session_id`)/`0030_employee_sessions` applied + verified live. CE-2: `{{COMPRESSION_CONFIG}}` safety net, `{{DELEGATION_CONFIG}}` (`delegate_task` in-turn isolation), tokenized `{{HOOKS}}`, `model-context.ts` (only place model metadata enters CE), a Python Hermes plugin `amtech-hygiene` (`transform_tool_result`/`transform_terminal_output`; secret-redact always, size-trim pathological bulk only — never nerf normal results), and a data-driven `routeForEventType` table. CE-3: `session-rotation.ts` with **pre-turn** `rotateSessionIfNeeded` + **post-turn** `recordSessionOccupancy` under the turn lock (3 sites), preserving `memory_session_key` while rotating `transcript_session_id`, carryover via `deriveNextAction`. CE-1 loose ends: tri-state primer claim (never silently never-primes). 87 files / 543 tests + plugins 10/10 green. Reprovision + live Hermes proof pending.
- [2026-07-12 23:00 — CE-2/CE-3 production implementation plan authored (docs-only)](2026-07-12-2300-ce2-ce3-production-implementation-plan.md) — Authored the production implementation plan after auditing merged CE-1 source + primary-source Hermes/Anthropic/OpenAI/GLM research. It identified the CE-1 primer cap as 2000 tokens, clarified business-brain/facts semantics, confirmed MCP does not bypass approval gates, and framed the CE-2/CE-3 build later implemented in the 23:30 source-wired pass.
- [2026-07-12 04:00 — Complete product page inventory: 12 new pages/states](2026-07-12-0400-complete-product-page-inventory.md) — Built every remaining product page in the AMTECH system, fixture-mode: public `/pricing` + `/terms` + `/privacy`; `/forgot-password` + `/reset-password`; `/welcome` onboarding success; owner `/agent/[id]/connect/[connector]` consent+result and `/agent/[id]/billing`; customer-facing `/e/[token]` estimate portal (accept + pay deposit — the other side of the marketplace); first-run activation (`FirstRun` in Today when the desk is empty, fixture employee `emp_ui_new`); shared `public-chrome.tsx`. Gates green: typecheck, lint, 90 files / 568 unit, build, ui:test. UI-only. Gotcha reconfirmed: new routes 500 with MODULE_NOT_FOUND/ENOENT vendor-chunks on a long-running dev server = stale `.next`, not code — `rm -rf .next` + restart; production build passed throughout.
- [2026-07-12 03:00 — Polish pass: mobile preview bottom sheet, real estimate document, front-door SMS demo](2026-07-12-0300-polish-pass-mobile-sheet-estimate-doc-sms-demo.md) — Mobile Work Surface preview is now a dismissible bottom sheet (hidden until selection, safe-area aware, desktop unchanged); the fixture estimate is a real contractor document (letterhead, line items + ink total rule, ASSUMPTIONS + red PLEASE-CONFIRM flag, approval-provenance terms) and doubles as the design template for a future `artifact-view.ts` port; front door gains a 4-line SMS demo strip (text → HELD FOR YOUR OK → "yes send it" → receipt); Daily Brief dated, chat voice bars, stat-strip border fix. Gates green: typecheck, lint, 518/518 unit, build, ui:test.
- [2026-07-12 02:35 — Full page inventory: error/404/loading, Admin fixtures, Review variants, MCP-UI fixture card, metadata](2026-07-12-0235-full-page-inventory-and-fixture-coverage.md) — Built every missing page/state after the identity restyle: owner-safe global 404/error/loading; Admin console now fully renders in fixture mode (two-account world, environment readiness, trial-readiness reports, simulated support actions — closes the data-catalog §9 admin gap); signed Review gains media (inline-SVG photos)/receipt/failure fixture variants; a fixture `ui_resource` schedule-confirmation card exercises `McpUiResource` end-to-end and doubles as the design target for the Manager-side `ui-resources.ts` restyle; per-page metadata titles + SVG favicon (create page split into server wrapper + `CreateClient.tsx`). Gates green: typecheck, lint, 84/518 unit, build, ui:test. UI-only; no Manager/shared/test changes; no live claims.
- [2026-07-12 02:10 — AMTECH visual identity applied across all web surfaces (UI-only restyle)](2026-07-12-0210-amtech-visual-identity-owner-surfaces-restyle.md) — First real design pass per `ui-handoff/`: Inter + IBM Plex Mono self-hosted, ink-on-white / white-on-red token layer (red `#e11d2a` the only accent), hairline-separated composition replacing floating rounded cards across front door/create/claim/login/Work Surface/signed Review/Admin; Daily Brief as the Today hero with segmented 36px stat strip; Review page gets an AMTECH SECURE REVIEW header, bottom-pinned 48px action bar with a 3px red rule when gated; emoji icons → mono text tags; owner-facing dev vocabulary removed from front door/login; fixture artifact HTML restyled as an AMTECH document. Fixed a pre-existing `ui:test` cold-start race UI-side (kicker reads "Starting" until hydration, so the smoke can't click pre-hydration). Gates green: typecheck, 84 files / 518 unit tests, lint, build, ui:test + screenshots. UI-only: no shared contracts, Manager code, tests, or approval semantics touched; live proof not claimed. Carry-forward: MCP-UI card CSS (`ui-resources.ts`) + real artifact document (`artifact-view.ts`) still wear the old look (Manager-side, coordinate); shared WorkResource renderer still open.
- [2026-07-12 22:19 — CE-1 profile context + native Hermes memory source-wired](2026-07-12-2219-ce1-profile-context-native-memory-source-wired.md) — Implemented CE-1 in source: profile generation now requires normalized `profile_context`, seeds Hermes-native `memories/MEMORY.md` and `USER.md`, renders workspace brain slots, wires a fail-open scoped `pre_llm_call` hook to Manager, adds a Manager-owned once-per-session primer gate (`0029`), caps the reference-shaped primer at ~2k estimated tokens with a 400k session target note, and fixes business-brain semantics so `get_business_brain`/MCP resource return an index/resource map while facts live at `amtech://manager/business-facts`. Gates green: typecheck, 84 unit files / 518 tests, build, lint, integration skipped cleanly. Live Hermes hook/runtime proof pending.
- [2026-07-12 00:30 — Context-engineering workstream planned (CE-1..CE-4)](2026-07-12-0030-context-engineering-workstream-planned.md) — Docs-only planning pass after deep Hermes substrate + best-practice research. Authored `second-half-plan/context-engineering/` (README + phase-ce-01..04). Core reframe (founder-corrected twice): the "business brain" is the whole integrated employee system (already assembled in `buildEmployeeSnapshot`), not a facts stub; and never inject a per-turn digest — prime with references once per session. Key finding: Hermes already has the native agent-side brain (MEMORY.md ~2200c / USER.md ~1375c auto-injected into the cached prompt; `session_search`; `memory` tool), cache-safe `pre_llm_call` injection, tunable `compression:`, `hooks:`, subagent delegation, and `mcp_servers` for any connector. Phases: CE-1 native-memory seed + reference-shaped live-state primer + fix render drops; CE-2 compression/hooks/turn-concurrency; CE-3 rotation-before-compaction + handoff; CE-4 (deferred) connector-agnostic + operator modes. No code changed; infra slice untouched.
- [2026-07-11 17:52 — Production networking/admin infra source-wired](2026-07-11-1752-production-networking-admin-infra-source-wired.md) — Implemented the safe production-environment foundation: Cloudflare DNS desired-state dry-run/apply-gated script, plugin-built Caddy wildcard DNS-01 path, Caddy/prod-env proof scripts, read-only admin Environment readiness over proof JSON, env/runbook/Phase 6 updates, and focused tests. No Cloudflare mutation, public DNS/TLS, provider webhook, model/runtime, or roles/delegation gate was upgraded.
- [2026-07-11 17:31 — Next implementation: production networking/admin/Phase 6 proof run](../second-half-plan/production-networking-admin-phase6-implementation-handoff-prompt.md) — Copy-ready, research-heavy implementation prompt for the next agent. It orients from the repo and the three two-way-surface docs, requires broad repo + external primary-source research before edits, and scopes the build to Cloudflare DNS desired-state tooling, Caddy wildcard DNS-01, local production-mirror tests, limited live-infra proof tiers, admin readiness changes for the now more production-level environment, and Phase 6 production-environment release gates. Delegated account-member roles remain read-now/build-last unless explicitly re-scoped.
- [2026-07-11 17:05 — Inbound two-way surface, production DNS/TLS, and roles design docs complete](2026-07-11-1705-inbound-two-way-surface-production-dns-roles-docs-complete.md) — Completed the docs-only two-way-surface pass from the 21:00 prompt: standalone Part A inbound map, cited Part B Cloudflare DNS-only + static `*.agents` + Caddy wildcard DNS-01 design, deferred Part C roles/delegation design on `account_memberships`, CODEGRAPH §3/§5/source-map updates, and no feature code/config/runtime/provider claims.
- [2026-07-11 21:00 — Next session: map the inbound/two-way surface, production DNS/TLS, and the roles model](2026-07-11-2100-inbound-two-way-surface-roles-and-production-dns-review-prompt.md) — Copy-ready next-session prompt (review + research + design + plan only, no build). Outbound infra is proven; now map the **inbound two-way surface** (owner or future permissioned user or provider webhook -> surface -> Caddy -> Manager -> Hermes turn -> Manager tools -> back out), design **production DNS/TLS/IPv4-v6** (wildcard `*.amtechai.com` vs API-driven zone; Caddy on-demand vs wildcard DNS-01; dual-stack; webhook ingress vs egress default-deny; firewall posture), and design the **roles/delegated-permission model** (net-new on today's single-owner `SMS_ALLOWED_USERS` base: `account_members` + per-role capability scopes; a delegated user like a secretary acts in-scope but risky/out-of-scope actions raise an **owner-SMS approval** — requester != approver — reusing the approval gate + channel-router + `resolve_approval`). Part A = review the already-built inbound spine (webhooks + ingress two-door + turn-queue + wake + channel-router + preview links; cite files, find load fragilities). Part D sequences it: this design -> finish MVP + live providers -> finish admin/billing -> limited real-VPS tests (5-100/pod, not blitzscale) -> THEN build roles. Deliverables: `docs/production-networking-and-dns.md` + `docs/roles-and-delegated-permissions-design.md` + the inbound-path map + the sequenced plan.
- [2026-07-11 19:00 — Pod Alpha orchestration substrate proven on a real Docker host: lifecycle + DNS/routing](2026-07-11-1900-pod-alpha-lifecycle-dns-routing-proven-on-host.md) — Reframed from "close the tool loop" to **production infrastructure engineering, no LLM** (founder steer): provision/teardown/reinstate employee containers, run many concurrently, prove Docker DNS + Caddy routing between the fixed core and the dynamic fleet. **Root-cause reframe:** the exit-111 crash-loop was a **stale orphan** (old launcher, `CapAdd=[]`), not a live launcher bug — the current cap set is correct (proven by a running container); DNS was unwired only because `amtech_runtime` didn't exist + `HERMES_DOCKER_NETWORK` was empty. Done on-host (Docker 29.5.2): created `amtech_runtime` (compose `external: true`), pinned `hermes-agent:0.18.0` (image self-reports v0.18.0), GC'd 9 stale orphans (profiles preserved), relaunched employee onto the network (**alias resolves, gateway reachable, `employee→manager:8080/health`=200 from a peer**), built + brought up the **compose core (manager/web/caddy all healthy)** + 3 employees all on `amtech_runtime`. Proofs in `infra/proofs/`: **`deploy-smoke` PASS 8/8** (incl. `caddy validate` + **Caddy resolves the employee by Docker DNS**), **`caddy-proof` PASS**, **`capacity` PASS tier 5** (5 concurrent employees, 0 DNS failures — small honest tier on a 4-CPU/8GB box, NOT the real 20-25), **`egress-policy` dry_run** (default-deny `DOCKER-USER` design). Fixed 3 proof-script bugs (deploy-smoke `redirect:manual` for the proxy's 308→HTTPS; capacity `pssKb` EACCES guard + `busybox`→`alpine` DNS probe that lacked `getent`). **Deferred/pending:** crash/reboot auto-recovery (`--restart=unless-stopped` set + verified, but this sandbox daemon doesn't fire restart-on-kill even for a control container → real-VPS proof owed), egress `--apply` (needs root), real capacity number (needs 64GB + root), LLM tool loop (out of scope). Baseline green: typecheck, focused unit (9), lint, build. Branch `infra/pod-alpha-lifecycle-dns-routing`.
- [2026-07-11 16:55 — Next session: production runtime & deploy — close the tool loop, prove the stack](2026-07-11-1655-next-session-production-runtime-tool-loop-handoff.md) — Copy-ready next-session prompt for the production runtime & deploy work from the two prior 2026-07-11 handoffs, plus one **scope decision: the local model bridge is dead** (not using/fixing/extending it — ignore `model-bridge*` refs; the core tool loop must close on a **real provider-backed Hermes model**, not the local shim; deferred follow-up to prune the dead `model-bridge*.mjs` + `model-bridge.test.ts`). Restates: P0 deploy scaffolding + Pod Alpha proof scripts are `source-wired`; `ops:caddy-proof` and `ops:reprovision-scoped-mcp` PASSED locally with real ids, but **`local:tool-loop-proof` still FAILS** (worker times out on prompt/runtime loop before producing the required tool-audit/artifact/approval rows) — the one true blocker behind every `pending` gate. Task order: (1) tool loop green on a real model, (2) prove compose stack starts + healthchecks on a VPS-like host, (3) run Pod Alpha proof scripts on the real VPS and capture `infra/proofs/*.json`, (4) capacity benchmark + egress default-deny. No code changed this step.
- [2026-07-11 16:50 — Pod Alpha runtime proof work and launch blockers](2026-07-11-1650-pod-alpha-runtime-proof-handoff.md) — Added the Pod Alpha operator proof layer: JSON-emitting `deploy:smoke`, real-container `ops:caddy-proof`, scoped-MCP reprovision, capacity benchmark, off-host backup/restore, red-health, and egress-policy scripts; hardened the Hermes container launcher for user-defined Docker networks and bootstrap capabilities; fixed profile rerender cleanup and turn-claim RPC ambiguity via migrations `0027`/`0028`; wrote the Pod Alpha runbook; `ops:caddy-proof` and scoped-MCP reprovision passed, but `local:tool-loop-proof` still failed because the warmed local worker path hit runtime/prompt limits before producing the required audit/artifact/approval proof rows.
- [2026-07-11 11:42 — Production runtime P0 source-wired + capacity notes synthesis](2026-07-11-1142-production-runtime-p0-source-wired-and-capacity-notes.md) — Implemented the first P0/P1 deploy/runtime source pass without claiming live acceptance: research-grounded deploy note, Compose scaffold for Caddy/Web/Manager, Next standalone output, env-driven Caddy upstreams, validated Caddy snippet activation with rollback, hardened/pinned employee Docker launcher (`HERMES_VERSION`, labels, restart policy, runtime network/DNS alias, log rotation), employee lifecycle helper + admin suspend/resume/disable wiring, OpenAI-compatible model-bridge `tool_calls` normalization/stream deltas, and `local:tool-loop-proof` for owner turn → Manager tool → artifact/approval/audit local proof. Focused tests, typecheck, build, lint, integration skips, and acceptance report all ran; full unit still blocked by the pre-existing Gmail Pub/Sub timeout (plus collateral hook timeouts under the hung parallel run; focused admin/artifact suites pass). Capacity notes synthesized as hypotheses: one employee per sub-$10M SMB, no swarm framing, don't prioritize sleep/wake orchestration now, start with pod-style 16-24 core / 64-128GB nodes and benchmark real employees per node before making capacity/pricing claims. Live VPS/Caddy/Hermes/provider proof, scoped-MCP reprovision proof, egress, backup/DR, and observability remain pending.
- [2026-07-11 11:20 — Branch consolidation → single `main` + wiki redesign merged](2026-07-11-1120-branch-consolidation-and-wiki-redesign-merge.md) — Git hygiene + one wiki-doc merge; no MVP source changed. Made `main` the single furthest-ahead line: it already was (identical to `phase5-…`, superset of the three merged `worktree-*` branches); the two divergent branches (`codex/first-live-test-note` +3, `worktree-hermes-alignment-corrections` +6) were early 07-03/04/05 exploratory work ~32–33 commits behind that `main` already superseded, so merging either would **regress** `main`. Archived their tips + the live-onboard worktree's uncommitted WIP as `archive/*` tags (pushed to origin), removed both worktrees, and **deleted every non-`main` branch local + on origin** — `main` is now the only branch. Merged the **98.css retro wiki-explorer redesign** of `index.html` (FILES manifest + explorer wiring verified intact). **Rejected the onboard-harness "approval work-verb" slice** (hardcoding tool→owner-verb strings for frontier models is tech debt; extends the questionable `VERB_BY_TOOL` allowlist; approval is already first-class via the approval gate/`ApprovalCard` — surfacing it as a fuzzy progress verb is redundant) — abandoned the feature branch; bits preserved in `archive/live-onboard-harness-wip`. **Open thread:** redesign the existing `VERB_BY_TOOL` allowlist model-natively rather than growing it. **Pre-existing bug found:** `gmail-pubsub.test.ts` "acks handler failures with 204" times out at 5s on clean `main` (8 pass / 1 timeout) — not mine; the "all unit tests green" claim is currently untrue.
- [2026-07-11 01:00 — Wiki refresh + production deploy readiness review + re-sequencing](2026-07-11-0100-wiki-refresh-and-production-deploy-review.md) — Docs-only pass; no code changed; tree green (76 files / 488 tests). Founder decision: **fix production deployability + core tool-loop before admin panel/billing** (Phase 5 is source-wired and sufficient); orchestration = **docker-compose** core + per-account employee containers. Wrote `docs/production-deploy-readiness-review-2026-07-11.md` (10 severity-tagged gaps: no supervision, undefined employee launch / unpinned `HERMES_VERSION`, no `caddy reload` after snippet write, no per-employee lifecycle/reboot recovery, no CI/deploy, no backup/DR, no observability, open egress, no graceful drain, plaintext host secrets — each evidence-cited) and `second-half-plan/production-runtime-and-deploy-roadmap-2026-07-11.md` (P0 deploy foundation → P1 core loop → P2 durability/observability/egress → admin+billing PARKED → live gate → launch). Refreshed the GTM wiki to current state: `second-half-current-and-future-state.md` (Phases 2–5 are source-wired, not future; added Connector Center/resurfacing/QBO/admin/MCP-UI + the production readiness gap), `product-ai-employee-context.md` (killed the "thin factory / almost no custom code" framing, fixed the docker-required policy, added a QuickBooks rung, and **added a "what Hermes does out of the box" section — estimate is the wedge not the ceiling**), `product-agent-platform-architecture.md`, `README.md` (second-half plan named as the current forward plan + a **"phases are plan-specific" caveat**), and root `CODEGRAPH.md`. Plus the ui-handoff Connector Center/resurfacing catch-up. Next (approved follow-up) = implement P0, starting with Caddy-reload wiring.
- [2026-07-10 23:41 — Tool-agnostic Connector Center + resurfacing projection](2026-07-10-2341-tool-agnostic-connector-center-resurfacing.md) — Added shared `ConnectionSurface` and `ResurfaceItem` contracts plus Manager projections over the existing employee snapshot, capabilities, surface envelopes, connector rows, and tasks. Owner Connected now renders generic connected-business cards before raw connector rows; Today/Daily Brief use resurfacing items for attention counts; Manager MCP resources and admin diagnostics expose the same projections with proof. No Business Brain/portal/CRM work and no live gates upgraded. Targeted proof so far: shared build, typecheck, and `employee-stream`/`admin-routes`/`materialization` unit tests pass; full gates still pending.
- [2026-07-10 22:32 — Deep review loose ends + Phase 6 groundwork](2026-07-10-2232-deep-review-phase5-loose-ends-phase6-groundwork.md) — Deep source review pass across the owner work loop and launch seams. Fixed Work Surface `inbound_events` starvation by scoping account/employee in DB before limits; fixed SMS/web approval resolution so approved/rejected gates wake the employee through the normal owner-turn/runtime path; fixed admin account detail runtime visibility by scoping before limit; closed lower-severity QBO carry-forwards (refresh contention waits through the lease, lookup cache bounded, full/truncated 1000-row pages fail closed). Added regressions for every fix. Added a Save For Later / "No Later" research note for a future Resurface Ledger concept using reminders, daily briefs, materialization, SMS/web presence, and the business brain. No live gates upgraded: migrations 0022-0026, platform operator seeding, scoped-MCP reprovision proof, real Hermes tool loop, QBO sandbox proof, and egress control remain pending.
- [2026-07-10 16:30 — QuickBooks Online connector Phase A0-A6 source-wired](2026-07-10-1630-quickbooks-connector-phase-a-source-wired.md) — Built the native QuickBooks connector end-to-end from the earlier docs: a new Manager tool family `qbo.stub.ts` mirroring Gmail/Stripe. Connector lifecycle via `intuit-oauth` (owns refresh, guarded by an atomic `connector_accounts.token_refresh_lease_until` CAS); `qbo-client.ts` over `@apigrate/quickbooks` (Accounting-API only); `qbo-lookup.ts` name resolution with disambiguation (never guesses); four approval-gated write previews + `commit_quickbooks_write` as the sole write path; `query_quickbooks` + four reports. **The confused-deputy fix both reference repos leave open:** every write stages a `quickbooks_pending_writes` row and binds `approval_id` onto it (set once); commit verifies `row.approval_id === supplied approval_id` (not "some approval says approved"), CAS-claims `pending_approval→committing`, recomputes a payload hash, and its schema is a deliberate `.strict()` exception. `QBO_WRITE_ACTION_KEY_GROUPS` is its own peer array (not a send). CloudEvents-native webhook + external-door adapter (HMAC, per-realm fan-out, `inbound_qbo_events` dedupe, QBO text as untrusted data); capability-registry wired under a new `"accounting"` category (zero new UI). Migration `0026`, acceptance `run10-quickbooks.mjs`, `.env.example` QBO block, workspace policy notes, CODEGRAPH/wedge/docs updates. **Then ran an xhigh self-review and fixed 5 real issues before commit — the top one a production-breaker unit tests couldn't catch:** (1) CRITICAL — `canonical_payload` was `jsonb`, but jsonb reorders keys on round-trip so the commit-time payload-hash check would NEVER match → every live commit would fail; now stored as `text` (byte-identical), hashed as a string, parsed before the QBO call; (2) webhook now fans out to ALL connectors on a realm (realm_id isn't unique — `.maybeSingle()` would drop events when two accounts share one QuickBooks company); (3) query filter guard now rejects backslashes too (`\'` escape); (4) a DB failure after a successful QBO write no longer marks the row `failed` (returns ok/`persist_deferred`, `reqid` idempotency on the write); (5) `0026` now REVOKEs anon/authenticated grants. Gates green after fixes: typecheck, **74 files / 478 unit tests**, build, lint; integration 6/11 env-skipped. `source-wired`; no Intuit sandbox exercised (all mocked); migration `0026` not applied live. Deferred review follow-ups: contended-refresh poll window, qbo-lookup 1000-cap paging, unbounded lookup cache (all lower-severity, for Phase B). Deviations documented (run10 not run9; lease-column CAS not lock table; header paid-from AccountRef deferred; qbo-client test mocks the @apigrate boundary). Next = the live-testing phase: Intuit dev app + `QBO_*` creds + apply `0026` + sandbox proof via run10, re-verifying Intuit's CloudEvents/refresh-token policy live.
- [2026-07-10 13:30 — UI handoff packet refreshed + data catalog added](2026-07-10-1330-ui-handoff-packet-refreshed-and-data-catalog-added.md) — Docs-only refresh of `mvp-build/ui-handoff/` so a UI collaborator + their agents work from an accurate current picture instead of the Phase 2-era snapshot. Added `data-catalog.md`: full inventory of every data shape reachable by the UI (raw Manager rows → `ResourcePayload` → `SurfaceEnvelope`/`WorkResource`/`CapabilityGraphNode`), the concrete artifact-link/preview-image rendering mechanism (stored-file signed URL vs. generic HTML fallback vs. fixture HTML; `WorkResource.media`/`body_html`/`open_url`), MCP-UI generative cards, Admin console contracts, and a route map. Rewrote stale "Phase 3/4/5 planned" framing across the rest of the packet — signed mobile Review, Phase 4 materialization/capability registry, Admin console, and MCP-UI generative rendering are all `source-wired` and named as concrete existing surfaces now, not future work.
- [2026-07-10 15:00 — QuickBooks connector: research, architecture plan, then a heavy critique pass + fixes (docs only)](2026-07-10-1500-quickbooks-connector-research-and-architecture-plan.md) — Researched both reference QBO MCP repos in full plus QBO-platform/MCP-security research; wrote `docs/quickbooks-connector-architecture.md`/`quickbooks-connector-implementation-plan.md`/`quickbooks-api-gotchas.md`/`quickbooks-connector-implementation-handoff-prompt.md`. Core decision: `qbo.stub.ts` as a native Manager tool family mirroring `gmail.stub.ts`/`stripe.stub.ts`, entity-resolution-with-disambiguation, every write behind the owner-approval gate, CloudEvents-native webhooks. Then heavily critiqued (and fixed) a separately-drafted plan: settled the client-library question (`intuit-oauth` for OAuth, `@apigrate/quickbooks` for Accounting-API calls only — verified live against npm/GitHub, not `node-quickbooks`/not hand-rolled fetch), closed a confused-deputy gap in the approval↔pending-write binding (`quickbooks_pending_writes` table, `approval_id` must match the specific pending write, `commit_quickbooks_write` schema `.strict()` not the house-default `.passthrough()`), fixed an approval-policy naming mismatch (`QBO_WRITE_ACTION_KEY_GROUPS` as its own peer array, not folded into the send-specific `SEND_GATE_ACTION_KEY_GROUPS`), and confirmed (via `0001_init.sql`) `connector_accounts`' `unique(employee_id, connector_key)` still caps one QuickBooks company per employee. Also corrected framing: QuickBooks adoption isn't size-gated to "mature" $1-20M shops. Docs only; no `mvp-build` application source/schema/tests touched.
- [2026-07-10 11:53 — Phase 5 admin/ops source-wired](2026-07-10-1153-phase-5-admin-ops-source-wired.md) — Implemented the second-half Phase 5 operator/admin slice at production shape without claiming live acceptance: shared admin contracts/routes, migration `0025_phase5_admin_ops.sql`, Manager-only platform roles/support access/admin action tables, Manager admin read/action layer, `/manager/admin/*` routes, internal `/admin` web console with production browser-token gate, trial readiness report, support actions for suspend/resume/disable/needs-reprovision/MCP credential rotate+revoke/event repair, server-side redaction, support-reason audit, and owner-surface vocabulary cleanup. Local gates green: shared build, targeted admin/materialization tests, full typecheck, 68 files / 406 unit tests, lint, build, UI fixture smoke, integration suite env-skipped (6 files / 11 skipped). Live migrations `0022`-`0025`, platform-user seeding, reprovision proof, egress control, and real Hermes tool-loop/provider proof remain pending.
- [2026-07-10 10:37 — Four audit findings fixed](2026-07-10-1037-four-audit-findings-fixed.md) — Fixed four gaps a deep skeptical audit found beyond the existing `architecture-and-security-review-2026-07.md`: (1) centralized approval `action_key` gating into `packages/shared/src/approval-policy.ts` so a send-gate key structurally cannot be missing from the owner-auth-required set (was three independently-maintained literal sources); (2) migration `0024` closes an unhandled 23505 race in `claim_employee_turn_job[_for_employee]` when 2+ turns queue for the same employee concurrently (`ON CONFLICT DO NOTHING` on the lock insert); (3) `reapStuckTurns` now guards its requeue/fail updates with `lease_token`+`status='running'` (was unconditioned, could clobber a turn legitimately completing at the lease boundary); (4) new `isLocalRuntimeBackendAllowed()` admission check blocks provisioning on the `local` runtime backend (no container isolation) in production unless explicitly opted in. Local gates green: typecheck, 67 files / 400 unit tests (+17), build, lint; integration 6 files / 11 skipped cleanly (new race test included, not yet run live). Migration `0024` not yet applied live.
- [2026-07-10 10:22 — Next-session UI/product-operation review + Phase 5/6 planning prompt](2026-07-10-1022-next-session-ui-phase5-phase6-planning-prompt.md) — Copy-ready prompt for the next session to plan a deep review of the owner UI and product operation against tool-agnostic materialization principles, close Phase 4 production loose ends, implement second-half Phase 5 at production level, and lay reasonable Phase 6 groundwork. It includes startup read order, latest Phase 4/security context, UI/materialization audit questions, production Phase 5 scope, Phase 6 deferrals, verification expectations, and memory/codegraph update rules.
- [2026-07-10 01:16 — Agent trust-boundary hardening](2026-07-10-0116-agent-trust-boundary-hardening.md) — Implemented the pre-tenant A1/B2/A3 hardening pass from the architecture/security review: per-employee scoped MCP credentials (`0023` + `mcp-auth.ts`), no shared `MANAGER_INTERNAL_TOKEN` rendered into employee env/config, `/manager/mcp` derives identity from the scoped credential and rejects the global internal bearer, employee self-resolution is blocked for high-risk approvals, Docker launch gets immediate capability/resource/no-new-privileges hardening, and focused tests lock the boundary. Local gates green: typecheck, 65 files / 383 unit tests, lint, build; integration suite env-skipped (10 skipped). Live migration/reprovision/egress proof still pending.
- [2026-07-10 01:10 — Next-session architecture and review handoff prompt](2026-07-10-0110-next-session-architecture-and-review-prompt.md) — Copy-ready prompt for the next session focused on architectural work and general code reviews. It cites the latest Phase 4 memory handoff (`2026-07-10-0045-phase-4-source-wired-materialization.md`), includes the usual startup read order/protocols, calls out the current source-wired-only status and live proof gaps, and sets review focus areas for Phase 4 boundaries, RLS/secrets, MCP resources, SSE, turn drain, diagnostics, and generated shared dist behavior.
- [2026-07-10 00:45 — Phase 4 source-wired materialization](2026-07-10-0045-phase-4-source-wired-materialization.md) — Built second-half Phase 4 as a production-shaped source-wired capability/materialization layer: shared `SurfaceEnvelope`/proof/safety/render/capability contracts, Manager capability registry, generic materialization projection, MCP resources, diagnostics endpoint, migration `0022`, tuple SSE cursor, serialized SSE writes, atomic signed-link counter RPCs, secret-ref direct-read tightening, over-budget turn-drain fail/notify, and real-MCP-result proof classification. Local gates green: typecheck, 61 files / 373 unit tests, lint, build, ui:test; integration suite env-skipped (10 skipped). No live Hermes/provider/DB proof claimed.
- [2026-07-10 00:12 — Phase 4 hardening closure + next-session handoff](2026-07-10-0012-phase-4-hardening-closure-and-next-session.md) — Test-locked the deferred 4a-4d hardening items already present in source: artifact expired/revoked signed links return 410 while forged/wrong-scope stays 403; unresolved Stripe invoice webhook rows remain `processed=false` and replayable; signed-link `decodeSignedToken` exposes expired-but-valid tokens and `jti` is CSPRNG-backed; Gmail Pub/Sub handler failures still ack 204 but record `audit_log` failure rows. Targeted local gate green: 4 files / 30 tests. Next session should close the real Hermes tool-execution gate, finish production Phase 4 provider/runtime proof, and lay Phase 5 event/materialization groundwork where it shares those seams.
- [2026-07-09 22:10 — MVP hardening: 5 review fixes + accumulated stragglers + CRITICAL RLS closure](2026-07-09-2210-mvp-hardening-review-fixes-and-rls-closure.md) — Fixed the 5 Phase-3 code-review findings (dead "Open document" action → `WorkResource.open_url` anchor; expired-vs-invalid signed token → `decodeSignedToken` + 410 reissue UX; approval-preview money amount persisted on the approval `refs`; `createPreviewLink` false-success → `mustWrite`; deliver-path pre-dedupe orphan → binds moved after the `inbound_events` dedupe claim). Then swept accumulated stragglers from two adversarial reviews + the Supabase advisor: **CRITICAL RLS closure** — 21 public tables had NO RLS and were anon-readable cross-tenant (users, onboarding chat+phone, customer email bodies, audit, stripe_*); migrations `0018`/`0019` enable Manager-only RLS on all of them + `_migrations`, and `0020`/`0021` revoke the SECURITY DEFINER turn-queue RPCs from anon (they could claim owner turns + read message bodies) and grant only `service_role` — verified by `get_advisors` (no `rls_disabled_in_public` ERRORs) and `has_function_privilege`. Also: stuck-`running`-turn reaper (`reapStuckTurns` + `reap_stuck_turns` lane) so a crashed worker's owner message isn't silently lost; Stripe atomic webhook dedupe (`insertDedup`); `mustWrite` false-success fixes (gmail refresh-token persist, owner-session mint, front-door claim link); Twilio `StatusCallback` wiring; best-effort access counters; retention GC (`cleanup.ts` + `cleanup_expired` lane); `denyInternal` fail-closed unless `ALLOW_UNAUTH_DEV=1`; crypto `jti`; Gmail Pub/Sub failure record. Grounded expired-token + StatusCallback fixes in online research. Green: typecheck, **59 files / 356 unit tests**, lint, build, ui:test; live DB migrated `0018`-`0021`. Live employee tool-loop still bridge-blocked.
- [2026-07-09 18:15 — Phase 3 SMS signed previews/actions source-wired; live tool-loop still bridge-blocked](2026-07-09-1815-phase-03-sms-signed-previews-and-live-gate.md) — Built second-half-plan Phase 3 to production shape: signed, scoped, expiring preview/action links for approval/artifact/task/connector/job/work-event, rendered from the same read model as the web desk (`preview-render.ts` `buildWorkResource` over `buildEmployeeSnapshot`), with a token-only `POST /manager/preview/resolve` and an owner-authenticated `POST /manager/preview/action` (approve/reject reuse the idempotent `resolve_approval`; respond routes into the owner-turn pipeline; single-use `consumed_at`). Seeded the Phase 4 `WorkResource`/`WorkAction` contracts (Phase 3 is their first renderer); artifacts are now kind-agnostic (not PDF-only), closing the `storage_ref`-only signed-link gap. SMS injection attaches `descriptor.preview_url` in both wake+deliver paths so message and SMS carry the identical grammar-aware link; **no inbound keyword parser** (lean on the employee LLM's MCP `resolve_approval`); added a signed Twilio `/webhooks/twilio/status` callback. New mobile-first web `review` page (token-auth, sticky Approve/Decline/Reply) + fixture-mode screenshot. Migration `0017_preview_links` (Manager-only RLS) applied to live Supabase. Gates green: typecheck, **58 files / 346 unit tests** (+31), lint, build, `ui:test`. **Live gate honestly BLOCKED:** the reprovisioned employee (`emp_fcm1977…`, acct `acct_3t6yn02yc360oewyi2mvir`, runtime `:8771` /health 200) emitted the correct `create_estimate_artifact` MCP call as JSON *text*, but the throwaway model bridge doesn't run Hermes's tool-execution loop — DB shows no artifact/approval/preview_link and only a `provision_employee` (actor=manager) audit row; no faked proof claimed.
- [2026-07-09 11:45 — Next session: Phase 1/2 state and functional MVP roadmap](2026-07-09-1145-next-session-phase-1-2-state-and-functional-mvp-roadmap.md) — Consolidated handoff for the next session after the Phase 1 preservation, Phase 2 Work Surface, and UI handoff commits. Summarizes what was built, what is functionally source-wired, what is still not trial-ready, exact verification already run (`typecheck`, 53 files / 315 unit tests, `lint`, `build`), live/model blocker, pushed commits, and best next steps: close live/browser/runtime gate when model/provider capacity exists, browser-QA the Work Surface, start Phase 3 SMS signed previews, begin Phase 4 materialization/capability contracts, continue UI polish in parallel, and plan Phase 5/6 ops/billing/trial readiness. Updated with the UI-only fixture workflow: `NEXT_PUBLIC_AMTECH_UI_FIXTURES=1`, `npm run ui:dev`, `ui:browser`, `ui:test`, and `ui:test:headed` let a UI partner inspect/test the real web client without Manager/Supabase/Docker/Hermes/provider/model infrastructure; `npm run ui:test` now passes and writes desktop/mobile screenshots under `infra/.local/ui-fixtures/`.
- [2026-07-09 11:20 — UI handoff packet for parallel UI work](2026-07-09-1120-ui-handoff-packet-for-parallel-ui-work.md) — Added `mvp-build/ui-handoff/` so a UI-focused partner can work on owner-facing surfaces while MVP functionality continues. The packet includes read order/orientation, product grounding, wiki-vs-`mvp-build` distinction, current web UI source map, research/principles index, working protocol/memory rules, and an experimental future-surface backlog for signed SMS previews, preview images/media/video, artifacts, task progress, and cross-surface representations. Updated root and MVP READMEs to point to it. Web Work Surface remains the immediate priority. Docs only; no runtime/provider proof claimed.
- [2026-07-09 10:50 — Phase 2 Work Surface source-wired](2026-07-09-1050-phase-2-work-surface-source-wired.md) — Reviewed Phase 1 code and closed source-level loose ends: SSE approval-only cursor advancement, signed artifact-link access counts only after successful resolve/render, and hardened HTML fallback headers. Expanded the shared resource payload with employee/runtime/abilities/outputs/tasks, rebuilt the Manager snapshot read model from existing tables, and replaced the sparse web employee page with a multi-region Work Surface (Today, Chat, Jobs, Tasks, Outputs, Connected, Abilities, Activity, Settings-lite, and preview pane). Static gates are green; live browser/runtime acceptance remains pending because the temporary Claude-account bridge cannot currently run.
- [2026-07-09 02:15 — Phase 1 static gates + artifact fallback; live gate blocked](2026-07-09-0215-phase-1-static-gates-artifact-fallback-live-blocked.md) — Preserved interrupted Manager-as-MCP identity injection, Docker Manager-origin rendering, in-container terminal backend, broad persona, and local bridge tool-call compatibility. Wired `artifact-view.ts` as an escaped HTML fallback for payload-only artifacts and updated the web output route to render it. Static gates are green (`typecheck`, 52 unit files / 309 tests, `lint`, `build`). Local live gate remains blocked this session: sandbox port/IPC binding failed, then the user clarified the temporary Claude-account bridge is at its limit; no new runtime/provider proof ids claimed.
- [2026-07-09 01:45 — Second-half plan wiki/codegraph propagation + Phase 1 handoff prompt](2026-07-09-0145-second-half-plan-wiki-codegraph-propagation.md) — Updated root `CODEGRAPH.md`, `mvp-build/CODEGRAPH.md`, `wiki/README.md`, `wiki/product-agent-platform-architecture.md`, `wiki/principle-graph-materialization.md`, `wiki/principle-deliverable-driven-surfaces.md`, and `wiki/MVP/event-driven-office-and-generative-ui.md` to reflect the new second-half plan and Hermes GUI/materialization research. Added `wiki/MVP/second-half-current-and-future-state.md`, added a UI research coverage map to `mvp-build/second-half-plan/README.md`, and wrote `mvp-build/second-half-plan/phase-01-handoff-prompt.md` for the next implementation agent. Docs only; no runtime/provider acceptance claimed.
- [2026-07-09 01:15 — Hermes GUI surface research for AMTECH materialization](2026-07-09-0115-hermes-gui-surface-research.md) — Deep researched Hermes Workspace, Hermes WebUI, Nous Hermes Desktop, and Hermes Agent docs/runtime internals. Added `mvp-build/second-half-plan/surface-research-hermes-gui-and-materialization.md`, which frames AMTECH as the business-native materialization layer over Hermes: `SurfaceEnvelope`, `WorkResource`, `WorkAction`, `CapabilityGraph`, and `EmployeeEventStream`, rendered through web, SMS, signed previews, desktop/Deno, email, customer links, and admin without per-tool/per-connector rewrites. No source/runtime changes or acceptance claimed.
- [2026-07-09 00:30 — Second-half plan packet: tool-agnostic Hermes employee + serious owner surfaces](2026-07-09-0030-second-half-plan-tool-agnostic-owner-surfaces.md) — Created `mvp-build/second-half-plan/` as the fresh forward plan after reviewing codegraphs, current/old plans, recent memory, Hermes docs/repos, deployment/admin/metering notes, dirty-tree fixes, and the actual web UI. The plan explicitly says the backend is ahead of the owner product: web onboarding/login/Work Surface and SMS are not trial-ready. Seven phases total: Phase 0 current-state handoff, then preserve/close live gate, redesign owner Work Surface, SMS ambient inbox/link previews, tool-agnostic capability/rendering, trial ops/admin/billing, and free-trial/paid-pilot readiness. No source code changed in this step.
- [2026-07-06 01:30 — Live gate CLOSED: employee calls Manager tools end-to-end (+ bridge tool-forwarding, MCP identity injection, persona un-gating)](2026-07-06-0130-live-gate-closed-bridge-tools-mcp-identity-persona.md) — Ran the live gate on the 00:10 networking fix: a reprovisioned employee logs `MCP: registered 42 tool(s)` and actually calls `create_estimate_artifact` (real artifact in `audit_log`, actor=employee). Getting there required THREE more fixes: (1) the model bridge `buildWorkerPrompt` dropped the OpenAI `tools` array so the warm-Haiku worker could never emit a tool call — now lists tools + teaches the `tool_calls` JSON protocol (tool_turns 0->1); (2) the MCP server didn't bind employee identity — now `X-AMTECH-Account-Id/Employee-Id` headers are baked into `mcp_servers.amtech_manager.headers`, injected server-side into every call, and stripped from advertised schemas (model never handles ids); (3) softened `SOUL.md`/`AGENTS.md` to stop baking in refusals ("default to helping", keep only the money/outbound gate). **KEY GOTCHA:** employee tool calls are recorded in `audit_log` (actor=employee, tool:<name>), NOT `tool_invocations` (which only logs owner_chat_turn). psql absent — query via node+pg. Local Haiku-via-bridge calls tools but is inconsistent on multi-step chains (Opus 4.8 will be reliable). 304 unit tests green; not committed.
- [2026-07-06 00:10 — Fix: employee→Manager container networking (MCP url) + in-container terminal backend](2026-07-06-0010-container-manager-networking-and-terminal-backend-fix.md) — Fixed the tool-less-employee bug from the 23:43 handoff, at the profile RENDERER. (1) `employeeManagerOrigin()` rewrites host loopback (`localhost`/`127.0.0.1`) → `host.docker.internal` for docker-backend employees, so the baked `MANAGER_MCP_URL` + `MANAGER_API_ORIGIN` are reachable from inside the container (matches `--add-host=host.docker.internal:host-gateway`); `local`/production origins untouched; `DOCKER_MANAGER_API_ORIGIN` overrides. (2) New `TERMINAL_BACKEND` token (default `local`) decouples Hermes' in-container terminal execution from the Manager isolation tier — the container IS the sandbox, so no docker-in-docker; `RUNTIME_BACKEND` stays `docker` so the toolset policy still enables `terminal`. Verified via 8 new unit tests + a pure-function render smoke (real `.env`): `MANAGER_MCP_URL=http://host.docker.internal:8080/manager/mcp`, `TERMINAL_BACKEND=local`. Baseline green (typecheck, 299 tests, lint, build). **Live gate deliberately NOT run** (fix-from-reading, no stack relaunch); reprovision + drive an estimate to close it. Not committed.
- [2026-07-05 23:43 — Handoff: Manager must be central; fix host↔container networking so employees reach it (tool-less employee bug)](2026-07-05-2343-manager-centrality-and-container-networking-handoff.md) — Live test showed a provisioned employee runs TOOL-LESS: its rendered `config.yaml` points `mcp_servers.amtech_manager.url` at `http://localhost:8080/manager/mcp`, but inside Docker that is the container's own loopback, so the Manager MCP handshake fails ("registered 0 tool(s)") and it can't create estimates / connect Gmail / render MCP-UI — it just chats (audit_log has only provisioning; every tool_invocation is `owner_chat_turn`). Fix: renderer must emit `host.docker.internal:8080` for docker-backend employees (same host↔container class as the API_SERVER_HOST and model base_url fixes). Secondary: `terminal.backend: docker` fails inside the container (no docker socket) and gates terminal/file tools. Frames the **Manager as the central control plane** (event ingress/egress, tool calling via MCP, Work Surface/MCP-UI) and treats container→Manager reachability as a first-class invariant. Includes full repo-orientation + copy-paste session prompt.
- [2026-07-05 21:45 — Live headed test: model-bridge single-instance worker + owner-session/toolset gaps + live-test toolkit](2026-07-05-2145-live-test-model-bridge-single-instance-and-owner-session-gap.md) — **(updated)** Both blockers FIXED and the full loop verified (login→message→employee→bridge→Haiku→reply). Built a dev owner-login (`/manager/dev/mint-owner-session` + web `/api/dev/login`, gated by `DEV_OWNER_LOGIN`), fixed two provisioning regressions (`.env.tpl` `API_SERVER_HOST` 127.0.0.1→0.0.0.0 for Docker reachability; renderer `{{MODEL_CONFIG}}` now wires employees to the bridge via `HERMES_MODEL_PROVIDER` instead of hardcoded opus-4-8), recreated Ferraro as `emp_pnutiyn47n8g4rdagosl6u`, and shipped a **live-test toolkit** (`infra/scripts/local/test/`, `npm run live:up|status|reprovision|login|recover|down` + README + HANDOFF-PROMPT). Also shipped `6911400`: the no-key model bridge worker now holds ONE persistent warm Haiku stream-json session (not a fresh `claude -p` per parked request); helpers `toStreamJsonInput`/`resultTextFromEvent` + tests; verified end-to-end (one pid answered 3 turns incl. real onboarding JSON). **Run recipe under Node 26:** Manager must run with plain `npx tsx` (NOT `tsx watch` — v4.22.4 crashes on Node 26 resolving `@amtech/shared`); rebuild `@amtech/shared` dist first; ORCHESTRATOR→bridge is a launch override. Recovered the existing **Ferraro employee `emp_rz6k8puuv9xu1zzpiwygk0` ("Sage" / Ferraro Grounds & Gardens**, acct `acct_3t6yn02yc360oewyi2mvir`, port 8610) from a stale `gateway_state.json=running` dual-gateway crash (rm -f + mark stopped + `start-hermes-container.sh`). **Two blockers before tool testing:** (1) `owner_session_invalid` — `/login` is a stub, the httpOnly `amtech_owner_session` cookie is only set at onboarding; must mint a dev session (have account/user/SIGNING_SECRET). (2) Sage's rendered `config.yaml` predates the MCP-toolset fix (no `platform_toolsets.api_server`/`mcp_servers.amtech_manager`) so it has ZERO Manager tools — re-provision or patch its profile to test tools/connectors. Includes the full tool-display overview + which tools need no setup vs Gmail/Stripe.
- [2026-07-05 09:30 — Manager-as-MCP-server + Hermes toolset enablement + ToolActivityDescriptor spike](2026-07-05-0930-mcp-server-toolsets-tool-activity.md) — Fixed the verified toolset bug (rendered `config.yaml` had no `platform_toolsets.api_server`, so employees ran on `terminal/file/web` only): a safe-set policy (`packages/shared/platform-toolsets.ts`) tied to backend blast radius + provider-key availability, rendered via `{{PLATFORM_TOOLSETS}}`, plus `getToolsets()` + `npm run local:inspect` to prove the live surface. Made the tool surface **schema-first** (`tool-schemas.ts` zod = single source of truth; `runManagerTool()` shared dispatch validates + reuses handlers/gates). Built **Manager-as-MCP-server** (`mcp-server.ts`, `@modelcontextprotocol/sdk` web-standard streamable-http, `POST /manager/mcp`, `mcp_servers.amtech_manager` auto-attaches to api_server) — a transport over the proven registry, gates reused not reimplemented. Generalized the descriptor spine with `tool_activity` + `formViewFromJsonSchema()` so ANY tool materializes from schema with zero per-tool code (spike-proven on image_gen/session_search/a Manager tool); `ids.ts` made isomorphic so `@amtech/shared` is client-bundle-safe. 50 files / 283 tests, typecheck/lint/build green. **Live `/v1/toolsets` + MCP handshake + Hermes→Work live adapter pending** (funded model key + Docker host).
- [2026-07-04 19:12 — Local onboarding harness (bypass vs real-user) + live Twilio creds](2026-07-04-1912-local-onboarding-harness-and-twilio-creds.md) — Built the practical onboarding test harness from the `1840` handoff: varied contractor fixtures (`contractor-fixtures.mjs`), a REAL front-door path harness (`onboard.mjs`, `npm run local:onboard`) + Playwright form driver (`06-browser-onboard.mjs`), and a **gated dev phone-verify bypass** (`twilio.ts`/`identity.stub.ts`) that lets the real verify path run without SMS and **fails closed in production**. Bypass `bootstrap.mjs` now fixture-varied; RUNBOOK §6 separates BYPASS vs REAL-USER paths. Wired the founder's live Twilio trial creds (account/token/number/verify-service `VA…`/test-recipient/dev-bypass) into the gitignored `.env`; committed examples got placeholders only. 46 files / 263 tests, build, lint green. **One hard blocker remains for the full live loop: a funded orchestrator model key (`ORCHESTRATOR_API_KEY`/`XAI_API_KEY`/`OPENAI_API_KEY`) — same key gates onboarding conversation AND Phase 5 Hermes chat.** Next: add model key → capture `/v1/runs` transcript → Phase 5 `runtime-accepted` → Phase 7 metering instrumentation.
- [2026-07-04 14:40 — Phase 5: triage, batching, live stream, MCP-UI generative UI](2026-07-04-1440-phase-05-triage-batching-live-stream-mcp-ui.md) — Phase 5 built in full and `source-wired`: rules-first triage with priority + provider-burst batching → one `manager.digest` (migration `0016`, `flush_event_batches` lane); the polled snapshot replaced by a live Manager SSE stream (`employeeStream` + `employee-stream.ts`) woken by `progress-bus.ts` (in-process EventEmitter = the testable Realtime/NOTIFY stand-in) with cursor catch-up + poll fallback; Hermes `/v1/runs/{id}/events` streaming → owner-safe work-verbs (`work-verbs.ts`) as live progress; **generative UI adopted via MCP-UI** (founder call) — agent `view` → Manager-compiled `ui://` `rawHtml` `UIResource` (`ui-resources.ts`, `@mcp-ui/server`) rendered in a sandboxed iframe whose `postMessage` intents route through the approval gate (`McpUiResource.tsx`). 44 files / 254 tests, build, lint green. **Repo correction:** root `GTM-RESEARCH` is one git repo with GitHub remote `benamtech/ai-employee-v1` (not local-only). Live Hermes SSE + Supabase `0016` RLS proof pending.
- [2026-07-04 00:43 — Local no-SMS bootstrap restored](2026-07-04-0043-local-no-sms-bootstrap.md) — Recreated interrupted local infra work: `PROVISIONER_SKIP_SMS` guarded no-SMS provisioner path, local env examples prefilled for Supabase project `uxuruijrgghshfwnaagb`, real Manager-tool bootstrap, web-chat smoke script, per-employee Docker Hermes runtime wrapper, Docker-outside-of-Docker devcontainer, local acceptance scripts including browser Work Surface check, package scripts, and infra docs. Local gates green: typecheck, 38 unit files / 223 tests, lint, build, script syntax checks. Supabase shared-pooler `DATABASE_URL` works and migrations `0001`-`0015` are applied after fixing stale `0002` RLS table scoping. Blocked for live runtime run by this agent shell's stale Docker group; no live provider/runtime acceptance claimed.
- [2026-07-03 20:21 — Hermes capabilities/runs alignment implemented](2026-07-03-2021-hermes-capabilities-runs-alignment.md) — Implemented the Hermes alignment audit fixes: cached `/v1/capabilities` gate, Runs-first/Sessions-fallback execution, strict account+employee session key, external Hermes run id correlation on `work_runs`, queued owner-turn run closure in drain, live Hermes contract-test scaffold, and `0015` schema/doc updates. Local gates green: typecheck, 38 unit files / 223 tests, integration 10 skipped clean, lint, build.
- [2026-07-03 19:52 — Hermes integration alignment audit](2026-07-03-1952-hermes-integration-alignment-handoff.md) — Research-only checkpoint and prompt for the next session to audit current code against Hermes Agent v0.18.0's most efficient production surface: capabilities-first API Server integration, `/v1/runs` + SSE for Phase 5 live progress, Sessions chat as fallback, deliberate `X-Hermes-Session-Key`, Manager-owned product semantics, and profiles-not-sandbox caution. Adds `docs/hermes-agent-authoritative-record.md`; no source/code changes.
- [2026-07-03 19:25 — Phase 4 completion fixes + Phase 6 proof cleanup](2026-07-03-1925-phase-04-completion-and-phase-06-proof-cleanup.md) — Closed review findings after Phase 4/6 groundwork: ingress-owned and direct-owned `work_runs` now finish on success/repair/throw; drain lane now persists `to_owner` messages and preserves `run_id`; 0014 makes real turn-claim RPCs return `run_id`; Phase 6 status/RLS proof docs caught up. Local gates green: typecheck, 38 unit files / 216 tests, integration 9 skipped clean, lint, build.
- [2026-07-03 18:55 — Phase 4 hardening + Phase 6 metering foundation](2026-07-03-1855-phase-04-hardening-and-phase-06-metering.md) — TDD-hardened the Phase 3/3A/4-core spine before env (fake-supabase now enforces unique indexes + rpc; every new module + adapter contract tested; audit bugs fixed incl. a turn-queue orphan; drain lane added), then built Phase 6 metering (0013 ledgers + metering.ts best-effort + run_id threaded ingress→deliver→wake→turn→router→owner-turn). Two-door decision CONFIRMED (external→ingestEvent adapter; internal→deliverEmployeeEvent). 38 files / 214 tests; provider/runtime `pending`.
- [2026-07-03 17:47 — Phase 4/5/6 next course after live employee core](2026-07-03-1747-phase-04-05-06-next-course.md) — planning handoff after recent Phase 3/3A/4-core source wiring; recommends finishing Phase 4 proof/hardening, then Phase 5 stream/triage, with Phase 6 `run_id` metering foundation as the sensible parallel/next operating-layer move.
- [2026-07-03 13:32 — Phase 3 / 3A / 4-core source-wired](2026-07-03-1332-phase-03-03a-04-core-source-wired.md) — real Hermes Sessions client, DB-backed turn queue, generic ingress hot path, minimal channel router, and Gmail reply live wake core are source-wired; live runtime/provider proof pending.
- [2026-07-03 12:11 — Full architecture audit: Hermes boundary + three critical systems](2026-07-03-1211-architecture-audit-three-critical-systems.md) — verified readout: `runtime.ts` calls invented `/messages`+`/events/work` paths (native Hermes is `/api/sessions/{id}/chat`/Runs — adapter-vs-rewrite decision gates Phases 3A/4/5); event bus has real signatures+dedupe but no importance engine, Twilio inbound off-rail, dead registry, write-only batching; Work Surface rendering is descriptor-generic but all descriptors are Manager-hardcoded TS literals (wake path never invoked) and "SSE" is a one-shot snapshot; admin is planned-only (CLI+SQL today). Smallest moves to live-testable listed.
- [2026-06-30 10:30 — Agent-inbox + channel/session architecture (decision)](2026-06-30-1030-agent-inbox-and-channel-architecture.md) — source-of-truth doc `wiki/MVP/agent-inbox-and-channel-architecture.md`: universal "message to the agent" inbox (open source-adapter contract; source ≠ channel), conductor + Jobs + serialized inbox execution, presence-aware channel/session router, conversation-as-brain-artifact single thread, dedicated number per employee LOCKED. Reframes Phase 3 (universal ingress) / Phase 4 (brain reason-branch) + adds Channel/Session layer as a first-class concern. Open: verify Hermes mid-Run input semantics.
- [2026-06-30 09:45 — Commit split + Phase 3 ingress dedupe groundwork + seam assessment](2026-06-30-0945-commit-split-and-phase3-dedupe-groundwork.md) — split the mixed tree into 2 commits; added migration `0010` (unique `inbound_events.idempotency_key`) + `insertDedup` 23505 backstop; mapped 2/3/4/6 seams (Phase 2 source-complete; Phase 3 groundwork laid, registry-promotion is the build; named Phase 4 atomic-claim-before-wake + Phase 6 run_id-at-ingress seams).
- [2026-06-30 09:02 — Events/reminder mesh robustness hardening](2026-06-30-0902-events-mesh-robustness-hardening.md) — `events.stub.ts` taken happy-path → defensible: DB-fault helpers (`db.ts`), atomic reminder claim, migration `0009` idempotency index, scheduler actor guard, entitlement gating, tz-aware briefs. `source-wired`, **uncommitted** on branch `fix/events-and-systemic-robustness` (also still carrying uncommitted Phase 2).
- [2026-06-30 01:46 — Phase 2 runtime/scheduler source-wired](2026-06-30-0146-phase-2-runtime-scheduler.md) — Docker-default runtime backend, protected Manager scheduler runner, `hermes_job_runs` proof writes, `runtime_health_checks`; source-wired with live runtime gate pending.
- [2026-06-30 01:33 — Next session: tie up Phase 1, then build Phase 2 (production)](2026-06-30-0133-next-session-phase-1-loose-ends-and-phase-2.md) — forward orientation: finish/verify the Phase 1 acceptance system, then full production implementation of Phase 2 = **Runtime & Scheduler Productionization** (not the old artifacts phase).
- [2026-06-30 01:20 — Re-phasing + Phase 1 acceptance harness + local git repo](2026-06-30-0120-rephase-and-phase-1-acceptance.md) — forward plan re-authored as modular phases (1–13); Phase 1 live-acceptance system built + locally verified; `mvp-build` is now a local-only git repo.

---

## Durable memory writing protocol (when + how)

Write or update a durable memory handoff at these triggers. This is a **standing protocol** — follow
it without being asked.

### When to write

1. **Mid-session checkpoint — after substantial multi-feature or architectural work.**
   Before context grows long or a session ends, and any time you've completed a chunk that spans
   multiple files/features or changes a structure, write/update a dated handoff. Don't wait for the
   end; checkpoint so the next agent (or your future self) can resume cold.
2. **After a full phase implementation.**
   When a phase from [`../wiki/MVP/build-plan-current/phases/`](../wiki/MVP/build-plan-current/phases/)
   is implemented (even if its live gate is still pending), write a phase handoff: what was built,
   local proof, what's pending, what the next phase inherits — and cross-link the matching
   `implementation-records/` entry.
3. **After an architectural decision or a plan change.**
   Capture the decision, the rationale, and the seams it affects (e.g. the Workstreams→phases
   re-org). Decisions are easy to lose; record them here with the "why".

### How to write

- **File name:** `YYYY-MM-DD-HHMM-<kebab-slug>.md` — **date, 24-hour time, and a relevant title**
  (e.g. `2026-06-30-0120-rephase-and-phase-1-acceptance.md`). The time disambiguates multiple
  handoffs in a day and orders them; the title makes the file scannable without opening it.
- **Header:** title, `Date:` (with time), `Status:`, `Scope:` (one line each).
- **Body sections (in order):**
  - **What changed** — concrete, file-referenced.
  - **Why** — the reason/decision behind it.
  - **Current status** — use the acceptance vocabulary: `source-wired` / `provider-accepted` /
    `runtime-accepted` / `planned` / `pending`. Never claim a status without real proof.
  - **Files / seams touched** — representative paths; name the seams the next phase plugs into.
  - **Carry-forward / next** — what the next agent should do, blockers, open decisions.
  - **Verification** — exactly what was run and the result (commands + outcomes).
- **Update the index** above (one line, newest first) every time you add a file.

### Rules

- **Factual only.** Do not claim live external services were tested when they weren't (the Realness
  Rule). Local proof is local proof; the live gate is separate.
- **Living, not archive — with one exception.** Supersede/delete stale *decision/status* notes so the
  newest handoff reads as current truth. **Keep dated phase handoffs as history** (they are the
  development record), but make sure the index and the latest handoff reflect reality.
- **Link, don't duplicate.** Point to `implementation-records/`, `build-plan-current/phases/`, and
  `CODEGRAPH.md` rather than re-explaining them.
- **Keep it agentic-dev focused.** This is the handoff a cold agent reads to continue work — not a
  changelog, not a spec.
