# Prompt Compression Research

Status: active P0 research program with an executable forced-divergence envelope prototype; full Trace014 implementation remains unadmitted.

## Read order

1. `RESEARCH.md` — consolidated architecture and evidence boundary.
2. `LONGLMLINGUA_INTEGRATION.md` — method-by-method ACL 2024 transfer and repository adaptation.
3. `FORCED_DREAMING.md` — independent high-dimensional divergent exploration and delayed convergence.
4. `IMPLEMENTATION_PLAN.md` — compact execution plan for a coding agent.
5. `task.json` — experiment-compiler task input.

## Current implementation candidate

```text
query-conditioned coarse-to-fine Prompt IR compiler
+ mandatory authority/invariant-hypergraph closure
+ phase-local activation and position scheduling
+ dynamic relevance/risk budget ledger
+ protected atom/span compression
+ forced-dreaming divergent envelope
+ independent mechanism-family explorers
+ quality-diversity archive and delayed integration
+ tokenizer-specific renderers
+ deterministic expansion/recovery
+ component-level factorial benchmark
```

Executable research artifacts:

```text
compile-forced-dreaming.mjs
forced-dreaming.schema.json
```

Compile an exploration envelope:

```bash
node research/prompt-compression/compile-forced-dreaming.mjs \
  --task <task.json> \
  --out <forced-dreaming.json> \
  --count 8
```

The prototype emits independent exploration slots over a twelve-dimensional registered lens basis. It verifies control-vector separation before output and forces every explorer to produce mechanism, repository evidence, invariants, effects, predictions, falsifiers, patch bounds, and argv verification before convergence.

Conditional perplexity, contrastive scoring, latent/context-space steering, embeddings, symbols, hypervectors, lens vectors, novelty, and diversity remain P0 search/ranking/rendering treatments behind the deterministic trust boundary. They cannot establish repository authority, P2 correspondence, executable P3 evidence, or P4 acceptance.
